上一章我已经为我们的程序引入了预重试主题与真正失败重试的主题，大家也都清楚了，当缓存在内存中的某个 PopCheckPointWrapper 对象过期之后，其持有的 PopCheckPoint 对象就会被持久化到本地，也就是持久化到对应的预重试主题下；而当预重试主题中的 PopCheckPoint 对象封装的消息依旧迟迟没有被 ack，那么达到重新投递时间的消息就会被重新投递到真正的失败重试主题下，这样一来消费者客户端就可以到真正的失败重试队列中消费这些消息，这些内容大家应该都还有印象。当然，随着以上这些内容的展开，大家也逐渐思考了更多的问题。  
  
比如说预重试主题下的消息该怎么被 ack 呢？也就是说，已经持久化了的 PopCheckPoint 对象对应的消息该怎么被 ack 呢？还有，在预重试队列中达到了重新投递时间的消息，这些消息要怎么被投递到真正的消费失败重试队列中呢？除此之外，还有一个最重要的问题，那就是消费者客户端怎么才能到真正的失败重试队列中消费消息呢？这些问题至今都还没有被我们解决，那接下来，我们就逐一分析，一起寻找这些问题的答案。  
  
引入 PopReviveService 类  
  
其实面对刚才分析的几个问题，我们也不是毫无头绪，就比如说怎么把预重试主题下未被 ack 的消息投递到真正的失败重试队列中这个问题吧，实际上在上一章我们已经分析过了，预重试队列中存储的虽然是 MessageExtBrokerInner 消息，但是每一条消息封装的都是一个 PopCheckPoint 对象，而每一个 PopCheckPoint 对象又都封装了一批消息是否被 ack 的信息，如果达到了某条消息的重新投递时间，而这条消息却仍未被 ack，那这个时候，就可以把这条消息投递到真正失败重试的主题下了。这个逻辑在上一章我就为大家分析过，大家应该很清楚了。  
  
但是，我们目前也仅仅是有这样一个设计思路，很多细节问题都还没有讨论，就比如说未被 ack 的消息怎么被重新投递到真正的失败重试队列中呢？按照之前的分析， 那肯定就是先确定这条未被 ack 的消息，然后再从 CommitLog 文件中找到这条消息本身的内容，接着再把查询到的完整消息投递到真正的失败重试主题下(至于真正的失败重试主题是怎么定义的，这个在上一章也讲解了，大家应该很清楚) 。这么做的原因很明确，PopCheckPoint 对象封装的仅仅是每一条消息是否被 ack 的信息，并没有封装每一条消息本身的内容，所以肯定要先从 CommitLog 文件中查询到这条消息本身的内容，然后再重新投递这条消息。  
  
而我们设计的消息队列框架并不会直接从 CommitLog 文件中查询某条消息的具体内容，因为 CommitLog 文件中存储了所有主题下消息的信息，根本就没有便捷的查找方式。 在查找某条消息时，通常会根据这条消息所属的主题信息、以及主题下消费队列的 Id 和这条消息在消费队列中的具体偏移量，先从指定的消费队列中查询到这条消息的信息，然后得到该条消息的大小以及在 CommitLog 文件中的具体偏移量，这个时候就可以根据这些信息从 CommitLog 文件中获取这条消息本身的内容了 。这也就是说， 要想把预重试队列中没有被 ack 的消息重新投递到真正的失败重试主题下，必须要知道未被 ack 的消息所属主题信息、所在消费队列 Id、在消费队列中的具体偏移量 ，这样才能先从消费队列中查询到这条消息的信息，然后再从 CommitLog 文件中或者这条消息的真正信息，这个逻辑大家可以理解吧？  
  
如果大家理解了以上逻辑，那接下来就很好说了，因为在每一个 PopCheckPoint 对象中，都封装着待 ack 的每一条消息的主题信息、所在消费队列 Id 信息、以及在消费队列具体偏移量的信息，既然这些信息都可以直接从 PopCheckPoint 对象中获得，那么只要哪条未被 ack 的消息达到重新投递时间了，就可以直接根据 PopCheckPoint 对象封装的相关信息从 CommitLog 文件中查询到这条消息本身的内容，然后把这条消息重新投递到对应的重试主题下即可，而所谓重新投递，无非就是把消息交给消息存储引擎去存储而已，这之后的逻辑就不必我再展开分析了吧？  
  
好了，这样分析下来，上面提出的第二个问题似乎就快解决了，反正只要判断出预重试主题下的哪些消息还没有被 ack，并且这些消息到达重试投递的时刻了，那就可以执行刚才分析的操作，对吧？那这样一来，似乎就回到了老地方，之前我们重构 PopBufferMergeService 时为该对象实现了一个功能，那就是把缓存在内存中的，但是已经超时过期的 PopCheckPoint 对象持久化到本地，而现在我们要实现的则是把与重试队列中达到重新投递时间的，未被 ack 的消息投递到真正的失败重试主题中，这两个功能除了最后执行的操作不同，前面的操作可谓是几乎一样。因为不管怎样，程序都要从一堆数据中找到需要处理的数据，对吧？PopBufferMergeService 对象处理超时未被 ack 的 PopCheckPoint 对象的方法非常简单，那就是在循环中定期判断是否有数据达到要被处理的条件，并且 PopBufferMergeService 本身就是一个线程。 那现在我们要把预重试主题下的未被 ack 的消息重新投递了，那显然也应该启动一个线程，在循环中定期判断哪些未被 ack 的消息到达了重新投递时间，满足条件则重新投递这些消息即可 。这个逻辑大家可以理解吧？如果大家理解了这个逻辑，那接下来我就想引入一个新的类，也就是 PopReviveService 类，这个类的对象就负责把预重试队列中的消息重新投递到失败重试主题下，当然，根据刚才的分析， 这个 PopReviveService 类显然也要继承 ServiceThread 类，本身就是一个线程 ，然后把要执行的操作定义在 run() 方法中，这样该类的对象一旦启动就可以执行相应的操作了。  
  
好了，既然要引入这个 PopReviveService 类，也明确了这个类的职责，那接下来我们就要把这个类先定义出来，也许我们现在还不知道怎么给这个类填充具体的内容，但是至少先弄出一个空架子吧，就像下面代码快展示的这样，请看下面代码块。  
从上面代码块中可以看到，我们却是把这个 PopReviveService 类定义为了一个线程，并且给这个线程定义了启动后应该执行的 run() 方法，这个 run() 方法目前还未被实现。除此之外我还给这个 PopReviveService 类定义了 reviveTopic 和 brokerController 两个重要的成员变量，brokerController 成员变量的作用非常简单，因为在 PopReviveService 对象处理数据的过程中，肯定要判断哪些未被 ack 的消息要被重新投递，也就是判断每一个 PopCheckPoint 对象封装的消息标志位是否更新了。但是， PopCheckPoint 对象可以被持久化到本地了，要想判断这些数据，显然就应该先把本地数据加载到内存中，也就是说要从消息存储引擎的指定消费队列中查询具体的消息，然后把这些消息，也就是存储在预重试主题下的消息加载到内存中。既然要执行这个操作，显然就要用到消息存储引擎，而通过 BrokerController 成员变量恰好可以获得消息存储引擎 。 这个逻辑大家可以理解吧？当然，既然查询消息要用到消息存储引擎，那么把达到重新投递时间的消息投递到失败重试主题下，这个时候显然也要用到消息存储引擎，因为这个操作的本质就是把消息存储到消息存储引擎中，既然这样，肯定也要通过 BrokerController 成员变量获得消息存储引擎。这就是我为 PopReviveService 类定义 brokerController 成员变量的原因。  
  
至于定义 reviveTopic 成员变量的原因就更简单了，PopReviveService 这个线程要把预重试队列中的消息加载到内存，那肯定就需要从消费队列中查询消息呀，这个时候肯定就要知道消息所属的主题，也就是预重试主题，而这个主题是系统内置的，所以定义为 PopReviveService 类的成员变量，就可以随时使用了。看到这里大家也许会有其他的想法，从消费队列中查询消息时，不仅仅需要知道消息所属主题，肯定还要知道消息所属队列呀，只有知道了指定主题下指定 Id 的消费队列，才能从该队列中获取消息。可是，我们现在定义的 PopReviveService 对象只知道预重试消息的主题，并不知道要从哪个消费队列中查询消息。预重试主题是系统内置的主题，该主题下也有 8 个写队列和 8 个读队列，数量都是默认的。那现在新的问题就来了，我们要给 PopReviveService 类也定义一个 queueId 成员变量吗？ 如果定义了这个 queueId 成员变量，那就表示当前创建的 PopReviveService 线程对象只负责处理指定预重试主题下指定消费队列中的数据；如果不定义该成员变量，那么我们就可以在 PopReviveService 线程的 run() 方法中，让该线程循环处理预重试主题下 8 个消费队列中的数据 ，这个操作可以理解吧？那我们应该怎么做呢？显然应该仿照 RocketMq 源码中的方式， 给 PopReviveService 类定义 queueId 成员变量，这样一来就表示这个 PopReviveService 线程对象只处理预重试主题下指定消费队列中的数据 ，那其他消费队列的数据怎么处理呢？这就很简单了，只需要继续仿照源码， 在程序中创建 8 个 PopReviveService 线程对象即可，每个线程对象负责一个消费队列 ，启动这个 8 个线程对象之后，就可以处理预重试主题下 8 个消费队列中的数据了。我知道看到这里大家还会思考新的问题，比如说这 8 个 PopReviveService 对象该怎么创建，在哪里创建呢？我想说的是，这些问题并没有那么紧急，可以先放一放，接下来我们就按照刚才的分析，把 PopReviveService 类重构一下。请看下面代码块。  
上面代码块的内容很多，但是注释都很详细，只要大家按照从上到下的顺序认真阅读，肯定能掌握上面代码块的所有内容。当然，即便是重构成这样，和我提供给大家的第二十三版本代码一对比，大家就会发现，在文章中展示的这个 PopReviveService 类中的内容还是做了非常多的简化，把很多琐碎的操作都省略了，只把最核心的操作保留了下来。大家把文章中的内容都掌握了，一定要再认真阅读一下第二十三版本的对应代码，到时候大家会掌握更多丰富的细节内容。  
  
重构 AckMessageProcessor 请求处理器  
  
当大家阅读完毕上一小节的代码块之后，可能就只对一个问题感兴趣了，那就是持久化到预重试主题下的消息究竟该怎么被 ack 呢？这个功能我至今也没有实现，因此上面代码块中的 consumeReviveMessage() 方法也实现得特别简陋，毕竟省略了一个非常重要的功能。但大家千万别觉得这个问题很难解决，其实这个问题解决起来非常简单，我之所以留到现在才讲解，就是想把复杂的知识先展示给大家，等大家都掌握了，就可以一笔带过剩下的简单内容了。  
  
大家应该都还记得，当消费者客户端成功消费了一条消息之后，就会为这条消息回复一个 ack 响应给 Broker 节点，当然，虽说是 ack 响应，其实是一个 ack 请求，只不过我习惯把它成为响应。而这个 ack 请求首先会被 Proxy 节点接收到，然后再被 Proxy 节点转发给 Broker 节点，而当这个 ack 请求被 Broker 节点接收到后，最终就会被交给 AckMessageProcessor 请求处理器处理，这个逻辑大家应该还有印象吧？而 AckMessageProcessor 请求处理器处理 ack 请求的功能我之前也为大家实现了，为了帮助大家回顾之前的内容，我把相关的代码搬运到这里了，请看下面代码块。  
从上面代码块中可以看到，只要在最终 ack 消息的 appendAck() 方法中，把创建好的 AckMsg 对象交给 PopBufferMergeService 对象去 ack 即可，然后所有操作就结束了。看到这里大家肯定会有一个疑问，在上面代码块的 appendAck() 方法中，似乎已经默认 ack 消息一定会成功，但是现在我们已经知道了，ack 内存消息的时候并不一定会成功， 因为内存中过期的数据就会被持久化到本地，这样一来 AckMsg 对象可能就无法在内存中匹配到唯一的消息，显然就无法 ack 成功了(具体逻辑请大家到 PopBufferMergeService 对象的 addAk() 方法中查看，该方法会在多种情况下返回 false，表示没有在内存中 ack 成功消息) 。而这个时候，根据之前的分析， 肯定就应该到持久化到预重试主题的消费队列中匹配消息，然后 ack 消息 ，这个逻辑大家可以理解吧？但上面代码块展示的内容却并不是这样，所以，分析到这里，我们肯定应该对上面代码块中的 appendAck() 方法进行一番重构。那该怎么重构呢？很简单， 那就是把创建完毕的 AckMsg 对象也持久化到预重试主题下 ，就像下面代码块展示的这样，请看下面代码块。  
上面代码块的内容非常简单，就是简单的持久化 AckMsg 对象的操作，我相信每个朋友都能看懂。但是，就算看明白了上面代码块中的内容，大家可能也不清楚持久化 AckMsg 对象到预重试主题下的用意，接下来就让我来为大家解释一下。实际上是这样的，上面代码块的操作， 会把 AckMsg 对象持久化到预重试主题下，并且一定会把该对象持久化到和自己对应匹配的，等待 ack 的消息的同一消费队列中，这样一来，当 PopReviveService 线程在处理数据时，就会从本地把消费队列的数据加载到内存，然后根据 MessageExtBrokerInner 消息的标签判断当前消息封装的是 ack 对象还是 ck 对象，如果是 ack 对象，那就可以使用这个 ack 对象去以加载到内存的 ck 对象中匹配唯一消息，匹配成功则更新消息标志位 。这就是持久化到预重试主题下的消息被 ack 的整个过程， 而这些内容就被定义在了 PopReviveService 对象的 consumeReviveMessage() 方法中 ，但是我并不会在文章中再次重构该方法了，否则要展示的内容就太多了。我想说的是，虽然这些内容很多，操作琐碎，但是逻辑都很简单，都是在循环中判断、处理数据，都是很常规的流程，所以这些内容就留给大家去我提供的第二十三版本代码中查看吧。  
  
好了，到此为止，预重试主题下消息 ack 的功能也实现完毕了，如果说还有一个问题需要解答，那就是 PopReviveService 对象的创建方式。根据我们之前的分析，我们已经明确了，每一个 PopReviveService 线程对象都负责预重试主题下的一个消费队列，这也就意味着要创建 8 个 PopReviveService 对象，那这 8 个 PopReviveService 对象在哪里创建呢？很简单，就在 AckMessageProcessor 处理器中创建，请看下面代码块。  
到此为止，本章内容就全部结束了，大家已经可以阅读第二十三版本代码的所有内容了，和我提供的第二十三版本代码比起来，文章中代码块展示的内容真的简化了很多。所以大家在阅读第二十三版本代码的时候一定要更加认真，在脑海里把文章中缺少的细节内容补充完整。当然，虽然本章内容已经结束了，但是我还不想就此结尾，实际上，我们还有一个很重要的问题等待解决， 那就是消费者客户端怎么去真正的重试队列中消费消息呢 ？  
  
实现消费失败重试功能  
  
看到这里，大家肯定会感到奇怪，为什么明明本章内容已经结束了，但是还要在继续延申一个小节，实现消费失败重试功能，既然要实现该功能，为何不在前面就直接实现了呢？坦诚地说，这种情况之所会出现，是因为我在迭代代码的过程中犯了一点小错误。从内容的连贯性来看，本小节实现的内容显然和前面两小节的内容是一个整体，这一点大家肯定都能看出来，我也能看出来，但是在我当初迭代代码的时候，把本小节的功能迭代到第二十四版本代码中了，而该功能在第二十三版本代码中就应该出现了，这是我的一个小失误。现在我为大家单独实现这个小功能，只是想提醒大家，这一小节的展示的所有代码都在第二十四版本代码中，大家在阅读的时候不要搞错了。  
  
好了，让我们言归正传，一起实现消费失败重试功能吧。还是那句话，这个功能实现起来非常简单，请大家思考一下，消费者客户端消费普通消息的操作是在哪里执行的？如果大家对之前的内容还有印象，肯定能回忆起来，消费者客户端消费普通消息的操作就是在 PopMessageProcessor 请求处理器中执行的，就是在该处理器的 processRequest() 方法中，就像下面代码块展示的这样，请看下面代码块。  
从上面代码块中可以看到，在 PopMessageProcessor 处理器的 processRequest() 方法中又执行了 popMsgFromTopic() 方法，也就是上面代码块第 37 行代码对应的操作，执行到这里就意味着 Pop 模式的消费者客户端开始从消费队列中消费消息了。很好，现在我们已经回顾了消费者客户端消费消息的操作，那现在要实现消费者客户端消费失败重试主题下的消息，这个就很好办了。 我们已经知道了真正的失败重试主题的构建方式，而目前消费消息的是 Pop 模式的消费者客户端，该客户端可以消费指定主题下所有队列中的消息，那只需先得到当前消费者客户端要消费的消息的主题信息，然后构建该消息的失败重试主题，接着让消费者客户端到失败重试主题中消费消息不就行了吗 ？在 RocketMq 源码中就是这么设计的，实际上， 在源码中 Pop 模式的消费者客户端在正式消费消息之前，也就是消费普通队列的消息之前，都会先执行一个随机数操作，也就是随机获得一个数值，看看这个数值是否能被 5 整除，如果能被 5 整除，那么在消费者客户端消费普通队列消息之前，都要先到失败重试队列中获取消息，消费失败重试队列中的消息 。总结下来就是这样一个设计理念： 消费者客户端在正式消费消息之前，总会有百分之 20 的概率消费失败重试主题中的消息 ，就像下面代码块展示的这样，请看下面代码块。  
到此为止，我就把消息失败重试功能实现完毕了，并且从上面代码块中大家还可以看到，假如消费者客户端从普通消费队列中获取的消息数量不够，那么接下来还可以到失败重试主题中获取更多消息。总之，消费者客户端每次到 Broker 节点获取消息，总要尽可能多获取消息消费。如果大家想阅读和失败重试功能相关的更详细的代码，那么大家就可以到第二十四版本代码中查看对应的内容。当然，第二十四版本代码中还有很多其它的内容，等下一章更新完毕了，大家就可以阅读这个版本代码的全部内容了。  
  
下一章要实现的就是顺序消费消息的功能，这个功能实现起来非常简单，所以我在文章中也只会展示该功能最核心的内容，毕竟还有死信队列等内容也要展示。由此可见，下一章的内容比较零散，也非常简单，可以说几乎没什么真正重要的内容，只是对前面内容的查漏补缺。正如我前面说的，随着课程的进展，留给大家学习的重要知识越来越少了，大家尽可以放松下来，好了朋友们，我们下一章见！  
若有收获，就点个赞吧

---
*Source: https://www.yuque.com/u26328320/kxtdy3/qzx11dd9yl6wxgka*  
*All content belongs to its respective owners and creators.*