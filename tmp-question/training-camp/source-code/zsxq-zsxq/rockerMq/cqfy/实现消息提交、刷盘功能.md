  
实现消息定时提交功能  
  
在上一章我们已经实现了消息追加到内存映射缓冲区、或者是堆外内存缓冲区的功能，这个功能一旦实现，就可以解决以下两个遗留问题：  
1 DefaultMappedFile 对象的 flush() 方法的执行时机是什么时候？  
2 DefaultMappedFile 对象的 commit() 方法的执行时机是什么时候 ？  
当然，按照问题的解决顺序来说，我更倾向于先解决第二个问题，再解决第一个问题，因为在消息队列框架没有开启内存读写分离功能时，接收到的消息会直接被写入到 pagecache 中，这个时候当然就可以直接执行数据刷盘操作；但是在开启内存读写分离的情况下，Broker 节点接收到的所有消息都会先被写入到堆外内存缓冲区，只有执行了 commit() 方法，把堆外内存缓冲区的数据提交到 pagecache 中，才能执行数据刷盘操作。这样分析下来，显然数据刷盘操作是最后要执行的，虽然 RocketMq 源码中默认没有开启内存读写分离功能，但我们已经把这个功能实现了，索性就当作开启了，那么我们就需要先把 commit() 方法执行时机分析了，然后再分析 flush() 方法执行时机。  
  
那么 commit() 方法究竟该在哪里被调用呢？其实这已经不算是一个难题了，尤其是对看了很多框架源码的大家来说，这更算不上是一个难题。当 Broker 节点接收到消息时，在内存读写分离功能开启的情况下，所有消息都会被写入到堆外内存缓冲区，数据已经被写入到那里了，会堆积在那里，而执行 MappedFileQueue 对象的 commit() 方法就可以把堆外内存缓冲区的数据提价掉 pagecache 中。那我不管别的， 直接启动一个定时任务，定期执行 MappedFileQueue 对象的 commit() 方法，不就可以把堆外内存缓冲区的数据持续提交到 pagecache 中了吗 ？这个逻辑大家都可以理解吧？  
  
当然，也不一定非要使用定时任务，毕竟消费者客户端消费数据需要时效性，这就意味着我们不必尽快把堆外内存的数据提交到 pagecache 中，如果是这样的话， 那我们其实可以启动一个后台线程，这个线程启动之后，就会在一个循环中不断执行 MappedFileQueue 对象的 commit() 方法把数据提交到 pagecache ，当然， 在启动线程之后也要考虑到堆外内存缓冲区中没有数据的情况，如果没有数据就没必要让线程继续循环下去，所以我们可以再定义一个时间间隔，让线程每次执行完提交数据的操作之后，可以休息指定的时间，然后再进入下一次循环 ；与此同时也要考虑到另一种情况， 就算对外内存缓冲区中存在数据，但是数据比较少，没有达到规定的阈值，那这个时候也没必要执行提交数据操作 ，上一章我们已经分析过了，积攒的数据达到 4 个 4KB 大小，才能执行提交数据操作，要尽量做到批量提交数据。这个流程大家应该也很熟悉了吧？  
  
当然，针对我们刚才分析的最后一种情况，大家可能会有一个疑问，假如堆外内存缓冲区中的数据一直没有达到规定阈值，难道数据就永远不提交到 pagecache 中吗？当然不是这样，如果真的这么做，那消费者就永远无法消息数据了。实际上我还会定义一个兜底操作， 那就是定义一个最大提交数据时间间隔，每次提交数据的时候我都会记录最新提交数据的时间，在每次循环的过程中，我会让程序判断距离上次提交数据是否已经超过规定阈值了，如果超过了阈值，那么就算堆外内存中的数据的大小没有达到预设标准，也会被提交到 pagecache 中 。这就是 commit() 方法被调用的时机和流程。  
  
那说了这么多，该怎么用代码实现呢？比如说这个后台线程该怎么定义呢？很简单，就使用 ServiceThread 类就行了，大家肯定对 ServiceThread 类很熟悉了，我就不在文章中展示这个类的具体内容了，这个类的对象就是一个后台线程， 只要我们定义一个提交堆外内存缓冲区的类，让这个类继承 ServiceThread 类，那么后台线程就定义完毕了 。 比如说我们就定义一个 CommitRealTimeService 类，让这个类继承 ServiceThread 类 ，这样一来，提交对外内存缓冲区数据到 pagecache 的后台线程就定义完毕了，只要创建 CommitRealTimeService 类的对象，然后启动它即可。而其父类 ServiceThread 类中定义了 start() 方法，该方法一旦被调用，线程就开始工作了。这个逻辑大家都清楚吧？内容非常简单，接下来就让我为大家展示一下 CommitRealTimeService 类的具体内容，不过在具体展示内容之前，我还有一点内容要补充，那就是在 RocketMq 源码中， CommitRealTimeService 实际上是 CommitLog 的一个内部类 ，为了和源码保持一致，所以我也就把 CommitRealTimeService 定义为 CommitLog 的内部类了；除了这个内容之外，在源码中还有一些额外内容，那就是当 CommitRealTimeService 线程启动之后，线程并不是会一直在死循环中工作，它也会有结束的时候， 那就是当消息队列程序正常关闭的时候，这个时候线程就会退出循环，然后执行一个非常重要的操作，那就是会尽可能多地把堆外内存缓冲区地数据提交到 pagecache 中，也就是说，这个时候线程会进入另一个循环，在循环中不加限制地执行 MappedFileQueue 对象的 commit() 方法把数据提交到 pagecache ，至于循环多少次，这也是事先定义好的，这个循环数值就被定义在了 FlushCommitLogService 类中，并且这个类中就只有这么点内容，就只定义了一个成员变量，其他什么都没有。  
  
从名字上可以看出来，FlushCommitLogService 翻译过来就是刷新提交数据的组件，这个大家可以理解吧？如果大家理解了这个，那么接下来我就可以跟大家详细分析一下了， 实际上在 RocketMq 源码中，不管是提交堆外内存数据到 pagecache 中，还是异步把 pagecache 的数据刷新到硬盘、或者是同步把 pagecache 中的数据刷新到硬盘的操作，这三个操作都有各自的后台线程执行，也就是说，我们会为这三个操作都定义一个线程对象，并且这三个线程类都会继承 FlushCommitLogService 类，而 FlushCommitLogService 类则继承了 ServiceThread 类 。现在我们只不过是把提交堆外内存缓冲区的数据到 pagecache 中的线程类 CommitRealTimeService 定义完毕了，并且按照源码来看， 这个 CommitRealTimeService 类还要继承 FlushCommitLogService 类 ，这个逻辑大家可以理解吧？如果大家理解了这些逻辑，那接下来就请看看我已经实现完毕的 CommitRealTimeService 类，请看下面代码块。  
以上代码块中的内容非常简单，实现逻辑之前也都详细分析过了，我就不再重复讲解了。到此为止，这个 CommitRealTimeService 类就实现完毕了。当然，我知道现在大家肯定有一个疑问，那就是这个 CommitRealTimeService 类的对象该怎么创建呢？也就是说，我们应该分析 CommitRealTimeService 的创建时机，以及线程启动时机。当然，这个 CommitRealTimeService 数据提交器的启动时机其实没什么可说的，肯定是创建了 CommitRealTimeService 对象之后就直接启动，执行 start() 方法即可；所以最重要的还是分析 CommitRealTimeService 对象的创建时机？但我现在还不想分析这个问题，我想先请大家耐心等等，等我把异步刷盘线程和同步刷盘线程都实现了，再回过头研究这个问题。从数据被处理的顺序上来说，数据已经从堆外内存缓冲区提交到 pagacache 中了，当然就可以执行刷盘操作了。  
  
实现消息异步刷盘功能  
  
在 RocketMq 源码中，提供了两种方式把 pagecache 中的数据刷新到本地硬盘，一种是同步刷盘，一种是异步刷盘。为了跟源码保持一致，所以我们也会自己的消息队列框架实现两种刷盘方式，现在我要实现的就是消息异步刷盘功能，所谓异步刷盘，其实就是处理消息的线程不必等待消息刷盘完成，就可以返回处理其他任务。这么说可能有点模糊，那我就给大家解释得清楚一些，跟大家描述一下 RocketMq 源码提供的同步刷盘的流程： 实际上在 RocketMq 源码中，如果使用同步刷盘方式把数据刷新到本地硬盘，那么当消息被追加到内存映射缓冲区、或者是堆外内存中之后，执行这个操作的线程就会继续执行消息刷盘操作，并且会阻塞等待，直到消息刷新到硬盘之后，该线程才会结束阻塞，继续处理其他任务 。这样一描述，大家就知道同步刷盘和异步刷盘的区别了吧？而我之所以先实现异步刷盘，是因为异步刷盘的逻辑和刚才实现的 CommitRealTimeService 组件的逻辑差不多。  
  
请大家想一想，程序要执行刷盘操作的时候，数据是不是已经存放在 pagecache 中了？而且随着程序的运行，还会有源源不断的数据被堆积在 pagecache 中，既然这样，那我不妨就再启动一个后台线程，让线程在死循环中不断执行 MappedFileQueue 的 flush() 方法；当然，和提交数据到 pagecache 缓冲区的操作一样，每次循环的时间间隔，每次刷新数据到本地文件的最小容量阈值，以及最大刷新间隔的兜底机制都要考虑到。当然，当后台线程退出循环之后要执行的操作也要考虑到，也就是在一个新的循环中尽可能多的把 pagecache 中的数据刷新到硬盘中。  
  
这样分析下来之后，那我们就可以专门为异步刷新数据到本地文件的功能定义一个组件， 比如说就把这个组件定义为 FlushRealTimeService 类，并且这个类也要继承之前引入的 FlushCommitLogService 抽象父类 ，这样一来，这个 FlushRealTimeService 类才能成为一个线程。这个逻辑大家可以理解吧？如果大家理解了这个逻辑，那接下来就让我把实现完毕的 FlushRealTimeService 类的内容展示给大家，这个 FlushRealTimeService 类也是 CommitLog 的内部类，具体实现请看下面代码块。  
从上面代码块中可以看到，FlushRealTimeService 类中的逻辑几乎和 CommitRealTimeService 类中的逻辑一致，所以我就不再重复解释了，大家自己看看就行。当然，这个时候大家肯定也会好奇 FlushRealTimeService 对象的创建时机，还是和之前一样，请大家把这个问题放一放，先让我把同步刷盘的功能实现了，再回过头解决这个问题。  
  
实现消息同步刷盘功能  
  
很好，现在提交缓冲区数据到 pagecache 的功能、以及异步把 pagecache 中的数据刷新到本地硬盘的功能都实现了，接下来就该实现同步刷新 pagecache 数据到本地硬盘的功能了。在有了前面两个功能的铺垫之后，现在我也就直接一点，给大家把同步刷盘的组件展示出来了。就像大家猜到的那样， 在执行同步刷盘功能的时候，也是启动了一个后台线程，这个后台线程会把 pagecache 中的数据刷新到本地文件。当然，这个后台线程肯定也继承了 FlushCommitLogService 类，这一点大家肯定也想到了，在源码中，这个后台线程就被定义为了 GroupCommitService 。  
  
当然，仅仅知道了这些还远远不够，因为在上一小节我已经跟大家分析了同步刷盘操作的流程： 如果使用同步刷盘方式把数据刷新到本地硬盘，那么当消息被追加到内存映射缓冲区、或者是堆外内存中之后，执行这个操作的线程就会继续执行消息刷盘操作，并且会阻塞等待，直到消息刷新到硬盘之后，该线程才会结束阻塞，继续处理其他任务 。这样分析下来，那我们启动 GroupCommitService 后台线程之后，后台线程不断把 pagecache 中的数据刷新到本地文件，这么做的话，不还是异步刷新数据吗？根本没有体现出同步的过程  
  
而同步之所以是同步，就是要让处理消息的线程等待消息被刷新到本地后，才能处理其他任务，这对数据安全存储也提供了一层保障。那该怎么让处理消息的线程等待数据被刷新到硬盘后再返回呢？这就更简单了，使用 future 即可。上一章我们已经实现了把消息追加到缓冲区的功能，如果执行当前线程的操作把数据追加到缓冲区了，不管是追加到内存映射缓冲区还是堆外内存缓冲区，让当前线程紧接着执行一个操作： 那就是把正在处理的这批数据的刷盘操作封装成一个请求，把请求提交给 GroupCommitService 同步刷盘组件，然后返回一个 future 对象，当前线程得到 future 对象之后就可以调用 future 的 get() 方法阻塞自己，直到 GroupCommitService 组件处理完对应的请求，也就是把对应的数据刷新到硬盘，然后给 future 设置完成状态，这个时候被阻塞的线程就会被唤醒，然后继续处理其他任务 。这个逻辑应该很容易理解吧？这是非常常规的逻辑，我就不再展开了。当然， 这就意味着我们需要给 GroupCommitService 组件内部定义一个队列，这个队列专门用来存储封装了刷盘操作的请求，在 RocketMq 源码中把这个请求定义为了 GroupCommitRequest 类，这个类也是 CommitLog 的内部类 ，如果大家理解了这些逻辑，那么接下来就让我先把这个 GroupCommitRequest 类展示出来，请看下面代码块。  
好了，GroupCommitRequest 类也定义完毕了，根据我们之前的分析， 在同步刷盘的情况下，每当有数据要被刷盘时，就会把这个操作封装成一个 GroupCommitRequest 请求对象，提交给 GroupCommitService 对象的内部队列，然后 GroupCommitService 线程对象就会在循环中不断从队列中取出请求，处理请求，把数据刷盘，然后给请求对应的 future 对象设置完成状态 ，这个流程大家应该都很清楚了，就算我不讲解，大家应该也能预想到，毕竟在之前的框架源码中已经见到过很多类似情况了。当然，在 RocketMq 源码中还有一个小优化， 那就是给 GroupCommitService 类定义了两个内部队列，一个是写队列，一个是读队列，这两个队列存放的都是 GroupCommitRequest 请求对象，当外部线程提交刷盘请求时，会把 GroupCommitRequest 请求对象提交给写队列，而当 GroupCommitService 线程执行刷盘操作处理请求时，会把写队列和读队列交换，这样一来，GroupCommitService 线程就可以直接从读队列中获取请求对象，处理请求即可 。而源码中这么做的原因也很简单，那就是在执行同步刷盘操作时，永远保证一个队列还可以继续接收新的刷盘请求，这样程序执行效率更高。这些内容分析完毕之后，接下来就让我为大家展示一下我早就实现完毕的 GroupCommitService 类，请看下面代码块。  
上面代码块中的注释非常详细，我就不再重复讲解了，到此为止，我就把消息提交到缓冲区的功能、同步刷盘和异步刷盘的功能全部实现了。当然，我们还没有具体使用以上几个组件， 大家也不清楚 GroupCommitRequest 请求对象是怎么提交给 GroupCommitService 线程对象的写队列中的，也不知道 GroupCommitService 线程对象的创建时机 。这些问题都需要解决， 那接下来就轮到另一个组件登场了，那就是 FlushManager 数据刷新管理器 。  
  
引入 FlushManager 数据刷盘管理器  
  
实际上是这样的，在 RocketMq 源码中定义了一个 FlushManager 数据刷盘管理器，之前我们定义三个组件，也就是提交消息组件、同步刷盘组件、异步刷盘组件都会被这个 FlushManager 数据刷盘管理器管理。这个 FlushManager 管理器会在 CommitLog 的构造方法中被创建出来，也就是说，在程序启动的时候，这个刷盘管理器就会被创建了。而在 FlushManager 创建的时候，在 FlushManager 的构造方法中，我们定义的三个组件也会根据具体情况被创建出来。如果用户在配置信息中设置了使用异步刷盘，那么就会创建 FlushRealTimeService 对象，如果配置了同步刷盘，就会创建 GroupCommitService 对象。FlushManager 刷盘管理器会在 CommitLog 的 start() 方法中启动，在 FlushManager 启动的时候，会启动内部各个组件工作。这也就意味着，当 Broker 节点启动之后，存储引擎模块内部的各个组件就会开始工作了。  
  
说了这么多，那么 GroupCommitRequest 请求到底是怎么提交给 GroupCommitService 对象内部的写队列的呢？这个也很简单，其实在 FlushManager 刷盘管理器中还定义了一个 handleDiskFlush() 方法，每当有数据被成功追加到缓冲区之后，就会调用该方法，如果是同步刷盘，那就会把刷盘操作封装成 GroupCommitRequest 请求对象提交给 GroupCommitService 内部的写队列；如果是异步刷盘操作，那就会在 handleDiskFlush() 方法中唤醒异步刷盘的线程，通知该线程有数据到来了，可以执行刷盘操作了，当然，究竟是否执行刷盘操作，还需要在异步刷盘组件中判断 pagecache 中积攒的数据是否足够多。大家可以仔细品味品味这个逻辑，如果以上逻辑都理解了，那接下来我就为大家展示一下这个 FlushManager 刷新管理器的内容，该类也是 CommitLog 的内部类，请看下面代码块。  
接下来就只有最后一个问题了，那就是这个 FlushManager 刷盘管理器的 handleDiskFlush() 方法会在哪里被调用呢？如果大家对之前实现的 CommitLog 类的 asyncPutMessage() 方法还有印象，那大家就会记得，在 asyncPutMessage() 方法中把数据成功追加到缓冲区后，就会在最后执行 handleDiskFlush() 方法，执行数据刷盘的相关操作。而 FlushManager 刷盘管理器的 handleDiskFlush() 方法就会在这个时候被调用，请看下面代码块。  
好了朋友们，到此为止我就把 Broker 节点接收到消息，然后把消息刷新到硬盘的整个功能都实现完毕了，这中间的流程也梳理清楚了。当然，消息刷新到本地并不意味着消息存储的整个过程已经结束了，实际上，我们只实现了一半的功能，因为 Broker 节点要做的不仅仅是存储消息，还要能让消息被消费者客户端消费，而这就是我们接下来要继续实现的功能。没错，下一章我就会为程序引入消费队列，虽然我们还没有实现消费者客户端，但我们可以先实现消费消息功能。而这个功能，其实也是消息存储的一部分。  
  
到此为止，本章的内容就全部结束了，大家也可以去阅读第十四版本代码了。可以看到，本章内容虽然有点多，但是都很简单，所以我就没有像之前那样为大家详细分析，而是只简单梳理一下逻辑就展示具体代码了。我相信大家在结合文章阅读第十四版本代码的时候，也不会觉得数据刷新的功能有什么难度。这一章就当是用来给大家放松了。好了朋友们，这一章就到此为止吧，我们下一章见！  
若有收获，就点个赞吧

---
*Source: https://www.yuque.com/u26328320/kxtdy3/shbqxcy64c49ul99*  
*All content belongs to its respective owners and creators.*