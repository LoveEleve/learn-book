# S-4 熔断降级域 — 全视角提问

## 功能

1. `DegradeSlot` 和 `DefaultCircuitBreakerSlot` 谁先执行？同一资源会双重熔断吗？
2. 三种规则 grade(RT/异常比例/异常数)各触发哪种断路器？
3. 断路器状态机完整的状态集合和合法转换是什么？
4. 探测请求是谁放过的？只有一个还是多个？
5. 慢调用比例阈值和异常比例阈值是严格大于还是大于等于？
6. 规则里 `minRequestAmount` 的作用是什么？
7. 默认规则(`*`)与普通规则如何共存？优先级如何？

## 性能

8. 断路器统计用滑窗还是单个计数器？窗口长度是多少？
9. `tryPass` 的 CLOSED 快速路径做几次原子读？
10. 状态通知会阻塞请求线程吗？

## 并发

11. OPEN → HALF_OPEN 只有一个探测请求，靠什么保证？
12. 规则更新时旧断路器复用与否是否线程安全？
13. `onRequestComplete` 与 `tryPass` 并发时，状态会竞态吗？
14. HALF_OPEN 探测请求被后续槽 block 时怎么恢复？

## 扩展

15. 如何注册自定义状态观察者？观察者能拿到什么？
16. 新增一种断路器策略需要动哪些点？

## 边界

17. 探测请求自身异常算不算失败？
18. `statIntervalMs` 与 `timeWindow` 的区别？
19. 规则删除后断路器对象如何释放？
20. 状态通知里 `snapshotValue` 什么时候是 null？

## 演进

21. 旧式 `DegradeSlot` 与 `@since 2.0.0` 的默认槽是什么演进关系？
22. HALF_OPEN 兜底回调是为了修哪个 issue？
