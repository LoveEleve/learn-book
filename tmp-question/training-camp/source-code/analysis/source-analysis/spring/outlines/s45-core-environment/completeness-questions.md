# C-3 Environment 全视角验证

## 开发者视角
| # | 问题 | 答案位置 |
|:--:|------|------|
| 1 | @Value("${server.port}") 的值从哪来？多来源谁优先？ | §2 (查找链 + StandardEnvironment 默认来源) |
| 2 | -Dserver.port 为什么能覆盖 application.yml？ | §2 (systemProperties 在前) |
| 3 | ${missing} 解析不到会怎样？怎么配默认值？ | §3 (宽松/严格双模式 + :default) |
| 4 | 嵌套 ${a.${b}} 支持吗？转义怎么写？ | §3 (递归解析 + \\) |

## 架构师视角
| # | 问题 | 答案位置 |
|:--:|------|------|
| 5 | 为什么用"来源列表+优先级"而不是单一 Map？ | §1 关键设计 (动态调整+并发读) |
| 6 | Boot 外部化配置的 17 级优先级本质是什么？ | §1 (addFirst/addLast 堆积) |
| 7 | 我要接入配置中心(自定义来源)怎么做？ | §1 (PropertySource 子类) + §2 (getPropertySources 调整) |
| 8 | 为什么"首个非 null 即返回"而不是合并所有来源？ | §2 关键设计 (优先级语义) |

## 学生视角
| # | 问题 | 答案位置 |
|:--:|------|------|
| 9 | getProperty 内部"遍历→占位符→转换"三步分别做什么？ | §2 (查找链) |
| 10 | 为什么用 Part 列表+递归解析占位符而不是正则？ | §3 关键设计 |

## 覆盖: 10 问 / 3 身份 / 100%
