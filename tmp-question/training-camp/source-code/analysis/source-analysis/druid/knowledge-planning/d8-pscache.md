# D-8 PreparedStatementPool — per-connection LRU 缓存

> 项目: Druid (JDBC 连接池) | 🟡 Working / 1 篇 | PreparedStatementPool(202)+PreparedStatementHolder(111)+PreparedStatementKey(DruidPooledPreparedStatement L910)
> 基线: DRUID-PLAN D-8 (PSCache) — 前置: **D-1(holder 生命周期+close 清理, 已分析)** — 展开 LRU 复用; 对照: Hikari 无 PSCache (FastList 只跟踪)

---

## §0.8

- 🟡 Working，1篇 — 缓存结构(**LRUCache extends LinkedHashMap(maxSize, 0.75f, true)**[PreparedStatementPool:185-201]: accessOrder=true 保证访问序, **removeEldestEntry 超 maxPoolPreparedStatementPerConnectionSize(默认 10)**[Abstract:118] 淘汰并关闭最旧) → 借还路径(**prepareStatement[L355-365]: poolPreparedStatements 时 getStatementPool().get(key), 命中: in-use 且 !sharePreparedStatements 则返回 null 不用缓存[L58-60], hitCount++; 未命中: conn.prepareStatement 新建+incrementPreparedStatementCount[L369-373]; 归还: DruidPooledPreparedStatement.close[L153]→closePoolableStatement[L138]: clearParameters/clearBatch[L160-180]→pooled && poolPreparedStatements && exceptionCount==0 → put 入缓存[L174-176], exceptionCount>0 → remove[L183-186], 否则物理关闭 closeInternal[L190-193]) → 键与适配(**PreparedStatementKey: sql+catalog+methodType+resultSetType/Concurrency/Holdability+autoGeneratedKeys+columnIndexes/columnNames** 全参化 equals/hashCode[L910-989]; **Oracle 隐式缓存适配**: put 时 enterImplicitCache[L89-94], 命中时 exitImplicitCacheToActive[L64-66]; hit/miss 计数 + dataSource.incrementCachedPreparedStatementHitCount)
- 设计模式: [模式: LRU 缓存]—LinkedHashMap accessOrder; [模式: 键对象]—PreparedStatementKey 全参数化

---

## 01 提取

| Source | Step | Inferred Knowledge Point | Confidence |
|--------|:--:|------|------------|
| PreparedStatementPool.java:185,189,192 | LRU 结构 | **LRUCache extends LinkedHashMap(maxSize,0.75f,true)(L189) — accessOrder 访问序; removeEldestEntry(L192-200): size>maxPoolPreparedStatementPerConnectionSize 时淘汰+closeRemovedStatement** | High |
| DruidAbstractDataSource.java:118 | 容量 | **maxPoolPreparedStatementPerConnectionSize 默认 10(L118)** — per-connection 上限 | High |
| DruidPooledConnection.java:355,359,364 | 借出 | **prepareStatement(L355): new PreparedStatementKey(sql,catalog,MethodType.M1)(L359)→poolPreparedStatements 时 getStatementPool().get(key)(L364)** | High |
| PreparedStatementPool.java:54,58,62 | 命中 | **get(L54): holder.isInUse() && !sharePreparedStatements → null(L58-60); 命中 incrementHitCount+incrementCachedPreparedStatementHitCount(L62-63)** | High |
| DruidPooledConnection.java:369,373 | 未命中 | **new PreparedStatementHolder(key, conn.prepareStatement(sql))(L369)+incrementPreparedStatementCount(L370)** | High |
| DruidPooledPreparedStatement.java:153,187 | 归还 | **close(L153)→conn.closePoolableStatement(this)(L187)** | High |
| DruidPooledConnection.java:138,173,186 | 入池 | **closePoolableStatement(L138): clearParameters/clearBatch(L160-180)→pooled&&poolPreparedStatements&&exceptionCount==0 → statementPool.put(L173-175); exceptionCount>0 → statementPool.remove(L186-187); 否则 closeInternal(L190-193)** | High |
| PreparedStatementPool.java:82,89 | Oracle 适配 | **put(L82): isOracle&&useOracleImplicitCache → OracleUtils.enterImplicitCache(L89-94); get 命中时 exitImplicitCacheToActive(L64-66)** | High |
| DruidPooledPreparedStatement.java:910,923 | 键 | **PreparedStatementKey(L910): sql+catalog+methodType+resultSetType/Concurrency/Holdability+autoGeneratedKeys+columnIndexes/columnNames(L911-921), 构造 L923-968, equals/hashCode 全字段(L982-)** | High |

---

## 02-04 聚合+分类+聚类 (1篇)

**1篇理由**: PSCache 是单一缓存机制 — 1篇 (~42行) 按"结构→借还路径→键与适配"展开; 入池三分支引用 D-1(close 链已分析), 清理点(close 时 statementPool 关闭 L2121-2124)引用 D-5(已分析)。

### P1P2P3 分级

| # | KP | 🔴🟡🟢 | 为什么 |
|:--:|------|:--:|------|
| P1-1 | LRUCache 结构 (accessOrder+removeEldestEntry) | 🟡 | **为什么🟡**: 容量控制 |
| P1-2 | 借出路径 (命中/未命中/in-use 保护) | 🟡 | **为什么🟡**: 复用逻辑 |
| P1-3 | 归还入池三分支 (put/remove/物理关) | 🟡 | **为什么🟡**: 缓存正确性 |
| P1-4 | PreparedStatementKey 全参数化键 | 🟡 | **为什么🟡**: 复用匹配 |
| P2-1 | Oracle 隐式缓存适配 | 🟢 | **为什么🟢**: 驱动特有 |
| P2-2 | 与 Hikari 无 PSCache 对照 | 🟢 | **为什么🟢**: 取舍对比 |
| P3-1 | hit/miss 计数与统计 | 🟢 | **为什么🟢**: 监控支撑 |

### 深度分类

| Cluster | KP | 级别 | 为什么 |
|:--:|------|:--:|------|
| A | **LRU 结构** | 🟡 | 容器 |
| B | **借还路径** | 🟡 | 核心 |
| C | **键与适配** | 🟡 | 匹配与方言 |

> **Cluster A (§1)**: LinkedHashMap accessOrder + removeEldestEntry + 容量默认
> **Cluster B (§2)**: prepareStatement 借出 + close 三分支归还
> **Cluster C (§3)**: PreparedStatementKey 全字段 + Oracle 隐式缓存 + 计数

## 05 闭环结论摘要 (Pass 2 内化 — 假设→验证→结论)

| # | 机制 | 结论 (一句话) | 源码位置 |
|:--:|---|---|---|
| q1 | 零手写 LRU | accessOrder=true+removeEldestEntry(put 后触发) = 标准 LRU; 淘汰即关物理 statement, in-use 条目跳过强关 | PreparedStatementPool.java:143-175, 185-201 |
| q2 | 全参数键 | sql+catalog+methodType+全部创建参数 — 命中即物理兼容, 少一维就错误复用; Oracle 隐式缓存让位不重复持有 | DruidPooledPreparedStatement.java:910-989, PreparedStatementPool.java:64-66,89-94 |
| q3 | 归还卫生协议 | 干净→put / exceptionCount>0→remove / 非缓存→物理关; in-use 保护防两线程串参数 | DruidPooledConnection.java:138-205, PreparedStatementPool.java:54-72 |
| q4 | Oracle 自适应行预取 | **fetchRowPeak(历史最大读行)驱动 rowPrefetch 动态调优**: ≤1→2, >默认→默认(上限), 否则 peak+1 — 小结果集少预取, 大结果集不超默认, 减少网络往返不浪费 | DruidPooledPreparedStatement.java:500-534 |
| q5 | fetchRowPeak 峰值语义 | setFetchRowPeak **只增不减**(历史峰值) — 预取调整依据"曾经需要读多少行", 峰值保守估计需求 | PreparedStatementHolder.java:73-77 |

→ 引出 D-6: SQL Parser 体系 — PSCache 命中依赖 SQL 完全一致 (全参化键), parser 的参数化是另一条复用路径 (导航指针)
