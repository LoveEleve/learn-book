# R-23 内存淘汰 — 深审 REVIEW 记录 (六层深审 + 07 五维度)

## 六层深审 (Pass 1-3 全程)

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 1 | **行号偏差** | processCommand 内 rejectCommand 实测 **L4050** (初稿写 L4046-4049), pre_command_oom_state **L4059** (初稿写 L4050) | 大纲节 1 + pass2-q1 修正 |
| 2 | 通过项 | 行号穷举 130 处全验证 (evict.c 118 + server.h/config.c/server.c/db.c 12) — 11 个"FAIL"均为区间引用覆盖 (注释行/多行语句), 无真实偏差 | 记录 |
| 3 | 通过项 | 数字穷举: EVPOOL_SIZE=16 (L33) / CACHED_SDS=255 (L34) / LRU_BITS=24 (server.h:896) / RESOLUTION=1000ms (898) / LFU_INIT_VAL=5 (server.h:3537) / samples 默认 5 范围 1-64 (config.c:3163) / tenacity 默认 10 范围 0-100 (3164) / lfu-log-factor=10 (3159) / lfu-decay-time=1 (3160) / policy 默认 NO_EVICTION (3133) 全部 ✅ | 记录 |
| 4 | 通过项 | tenacity 公式复算: 50us×10=500us 默认; 1.15^89≈2.5e5 → 500×2.5e5=125s≈2min (注释一致 ✅); 满量程 24bit×1000ms≈194 天; LFU 16bit 分钟≈45.5 天 | 记录 |
| 5 | 通过项 | 八策略位域 (server.h:562-569) + 三分派路径 (evict.c:538/564-566/635-637) + ALLKEYS 来源 (577-581) 全验证 | 记录 |
| 6 | 推断标注 | LFU ~1M 访问/100 计数 (社区经验值); 194 天/45.5 天回绕 (推导) — temporal-trace.md 显式标注 | 记录 |

## 07 五维度

### 维度1 功能正确性
- 三态驱动链 (performEvictions → rejectCommand/aeTimeProc) 逐行验证
- idle 三打分 (LRU/LFU/TTL) + TTL 免值对象特例
- 幽灵键机制 (池不更新 + 选键时跳过)

### 维度2 性能
- 常数内存近似: 16 槽池 + 5 采样/DB
- cached sds 255B 复用 (免每键分配, 分析器实证)
- delta 实测法 (删前后 used_memory 差) 免估算误差
- tenacity 时间上限 + 16 键周期检查

### 维度3 内存
- overhead 剔除 (AOF/repl 缓冲) 防 DEL 反馈环
- 采样上限 (键 < samples×10 提前退) 防稀疏表浪费

### 维度4 一致性
- pre_command_oom_state 命令内共享 (EXEC/模块/Lua)
- propagateDeletion + notify "evicted" + tracking invalidation 三联动
- 从库输出缓冲每 16 键 flush (防积压)

### 维度5 负面空间 (已写入大纲 5 条)
- 不做全局精确 LRU / 不做逐键评估 / 不做预算动态调优 / 不做大对象优先 / 不做淘汰排序保证

## 结论
R-23 全部锚点行号 130 处验证, 6 闭环完成, 1 处行号偏差已修正。推断显式标注。待二次 REVIEW 复核。

---

# 二次深度 REVIEW (2026-08-13, 07 五维度轮换 + 内容深度)

## R1 维度1: 叙事桥 + 结构完整性

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 4 | 通过项 | 前置 R-21/R-1/R-22/R-33 (序号 10/7/11/1 < 12) 均已讲 ✅; 引出 R-18/R-9 (未来域 OK); 对照 R-22 (过期 vs 淘汰双回收) ✅; 五结构元素齐备 ✅; 桥到 R-18 ("淘汰后碎片怎么整理") 自然 ✅ | 记录 |

## R2 维度2: 锚点密度

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 5 | 通过项 | 大纲 ~40 锚点 (file:line) — 🟡B 标准 ≥4, 大幅超 ⏫ | 记录 |

## R3 维度3: 前向引用

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 6 | 通过项 | 前置声明无未来域 (R-21/R-1/R-22/R-33 均序号 < 12) ✅ | 记录 |

## R4 维度4: 横切关注点覆盖

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 7 | **覆盖缺口** | 大纲节 6 未提 stat_evictedkeys (L682) 与 stat_last_eviction_exceeded_time (L750-759) — 淘汰统计面断链 (INFO 可观测) | 大纲节 6 补统计面 |
| 8 | 通过项 | 复制 (从库缓冲 flush/ignore_maxmemory/propagateDeletion) / 通知 ("evicted") / tracking (L4043) / 共享整数 (节 5) / LRU-LFU 写面 (节 3/4) — 全覆盖 ✅ | 记录 |

## R5 维度5: 负面空间 + 开篇质量

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 9 | 通过项 | 负面空间 5 条 ✅; 开篇具体场景 ("刚访问过的键还是被删/8 位计数器模拟百万访问") ✅ | 记录 |

## R6 内容深度: 反写测试 + 机制逐句对源码

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 10 | **表述错误** | 大纲节 1 "只读命令放行" — 实测拒绝条件是 **CMD_DENYOOM 标志** (server.c:3958: `is_denyoom_command = cmd_flags & CMD_DENYOOM`), 非"只读"; DEL/EXPIRE 等清理类写命令也无 DENYOOM 可执行 | 大纲节 1/数据流 + pass2-q1/pass1-notes 修正 |
| 11 | 通过项 | 其余 ~40 句机制描述逐句对源码一致 ✅ (idle 三打分 L152-168 / 幽灵键 L617-628 / 采样上限 L589-598 / 回绕处理 L72-80 / LFU 概率公式 L281-289 / 八策略位域 server.h:556-569 / tenacity 三级 L479-494 / delta 实测 L674-681 / 每 16 键三检查 L692-720) | 记录 |

## 二次 REVIEW 汇总

07 五维度轮换 + 内容深度共 **2 处新发现** (覆盖缺口 1 / 表述错误 1), 全部修复。反写测试结论: 大纲机制面完整可支撑写作。
