# R-26 t_list+blocked — 深审 REVIEW 记录 (六层深审 + 07 五维度)

## 六层深审 (Pass 1-3 全程)

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 1 | 通过项 | 行号穷举 117 处全验证 (t_list.c 41 + blocked.c 72 + server.h 10 + config/db/stream 4) — 15 个"FAIL"均为多行语句偏移的区间覆盖, 无真实偏差 | 记录 |
| 2 | 通过项 | 数字穷举: 10 种 btype (server.h:398-407) / blocked_clients_by_type 数组 (L1951) / list-max-listpack-size 默认 -2 (config.c:3152) 全验证 | 记录 |
| 3 | **机制洞察** | **唤醒只需 dbAdd 路径**: t_list.c 无 signalKeyAsReady 调用 (grep 证实) — 空键不存在不变量 (pop 空即删键 L748-750) → 阻塞者等待的键必然不存在 → push 走 dbAddInternal (db.c:192); stream 例外 (t_stream.c:2083 显式) | pass2-q2 重写 (初稿误判"需继续验证") |
| 4 | 通过项 | 机制验证: 双编码 GROWING/SHRINKING 分派 / XX 特判 / COUNT 范围 / 双向注册互链 (list node 作 value) / 三级快检 / dict 防重 / 换列表防递归 / 类型匹配 / PENDING_COMMAND 重处理 / 超时 null 回复 全验证 | 记录 |
| 5 | 推断标注 | 空键不变量命名 / stream 例外动机 / FIFO 公平性 — 3 处显式标注 temporal-trace.md | 记录 |

## 07 五维度

### 维度1 功能正确性
- push/pop 双态 (单/COUNT) + 空键处理
- 阻塞双向注册 + nokey 引用计数
- 类型匹配防误醒

### 维度2 性能
- blocked_clients_by_type O(1) 快检
- 双向节点关联 O(1) 解链
- ready_keys dict 防重 (脚本多 push 一次唤醒)

### 维度3 内存
- 空 list 即删 (不浪费)
- blocking_keys 空列表即清

### 维度4 一致性
- FIFO 公平 + 防递归 + 换列表连环唤醒
- PENDING_COMMAND 重处理闭环
- master 复制流不可阻

### 维度5 负面空间 (已写入两篇各 5 条)
- 不做双向索引 / 不做消息确认 / 不做优先级阻塞 / 不做多键部分唤醒 / 不做超时精确性

## 结论
R-26 全部锚点行号 117 处验证, 8 闭环完成, 1 处机制洞察修正 (唤醒路径)。推断显式标注。待二次 REVIEW + harness。

---

# 二次深度 REVIEW (2026-08-13, 07 五维度轮换 + 内容深度)

## R1 维度1: 叙事桥 + 结构完整性

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 6 | 通过项 | 前置 R-5/R-19/R-21/R-20 (序号 < 17) 均已讲 ✅; 01→02 承接 (等不到数据谁在等) ✅; 引出 R-10/R-16 (未来域 OK); 五结构元素齐备 ✅ | 记录 |

## R2 维度2: 锚点密度

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 7 | 通过项 | 01 篇 ~25 锚点 / 02 篇 ~22 锚点 (🔴A 标准 ≥8) ⏫ | 记录 |

## R3 维度3: 前向引用

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 8 | 通过项 | 前置声明无未来域 (R-5/R-19/R-21/R-20 均序号 < 17) ✅ | 记录 |

## R4 维度4: 横切关注点覆盖

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 9 | 通过项 | 复制 (LMOVE/BLMOVE 原样传播 + pop COUNT 重写) / 通知 (lpush/rpush/del) / 事件循环 (beforeSleep 消费就绪队列) / 脚本 (防重注释 MULTI 场景) — 全覆盖 ✅ | 记录 |

## R5 维度5: 负面空间 + 开篇质量

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 10 | 通过项 | 两篇负面空间各 5-6 条 ✅; 开篇场景化 ("LPUSH 不存在键/BLPOP 永远等不到非空 push/阻塞时客户端在干嘛") ✅ | 记录 |

## R6 内容深度: 反写测试 + 机制逐句对源码

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 11 | **行号猜测** | 01 节 4 "lmoveCommand (L908+)" — "+" 是猜测, 实测 **L1151**; blmoveGenericCommand **L1265-1280** 是"先试后阻"显式样板 (key==NULL → blockForKeys L1274 / 常规 lmove L1279) | 修正 + 补样板 |
| 12 | **行号偏移** | blockClient 内四行全偏一位: CLIENT_BLOCKED **L75** (写 76) / btype **L76** (写 77) / blocked_clients++ **L77** (写 78) / by_type **L78** (写 79) / addClientToTimeoutTable **L79** (写 80) | 大纲 02 + pass2-q5 全量修正 |
| 13 | 通过项 | 其余 ~45 句机制描述逐句对源码一致 ✅ (双编码 GROWING/SHRINKING/空键不变量/先试后阻/双向注册互链/三级快检/防重/换列表/类型匹配/PENDING_COMMAND 重处理/超时 null) | 记录 |

## 二次 REVIEW 汇总

07 五维度轮换 + 内容深度共 **2 处新发现** (行号猜测 1 / 行号偏移 1), 全部修复。反写测试结论: 两篇大纲机制面完整可支撑写作。
