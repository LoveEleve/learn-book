# R-28 networking 协议 — 深审 REVIEW 记录 (六层深审 + 07 五维度)

## 六层深审 (Pass 1-3 全程)

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 1 | 通过项 | 行号穷举 116 处全验证 (networking.c 108 + server.h 6 + config.c 2) — 7 个"FAIL"均为多行语句/注释偏移的区间覆盖 (L128-129/L305-306/L1616-1623/L1687-1694/L1934-1935/L2370-2371/L2424-2425), 无真实偏差 | 记录 |
| 2 | 通过项 | 数字穷举: PROTO_IOBUF_LEN=16KB (server.h:164) / REPLY_CHUNK=16KB (165) / INLINE_MAX=64KB (166) / MBULK_BIG_ARG=32KB (167) / NET_MAX_WRITES=64KB (106) / proto_max_bulk_len 默认 512MB (config.c:3206) / client_max_querybuf_len 默认 1GB (3226) / argv 起步 1024 增长 2× 上限 INT_MAX (L2337,2417) 全部 ✅ | 记录 |
| 3 | 通过项 | 机制验证: 五拒条件 (L281-296) / 双缓冲切换 (L328) / 尾节点续写 (L349-359) / 大参数零拷贝四前提 (L2424-2427) / 未认证三级限 (L2323,2375,2745) / 从库 replBufBlock (L1919-1942) / master repl_applied trim (L2635-2639) 全验证 | 记录 |
| 4 | 推断标注 | 静态 buf "大多数回复零分配" / 32KB 阈值权衡 / 16384 与 16KB 相关性 — 3 处显式标注 temporal-trace.md | 记录 |

## 07 五维度

### 维度1 功能正确性
- 三行状态机 (*→$→data) 全分支验证 (错误路径: 非 * / 非 $ / 超限 / 未认证)
- 零拷贝路径前提 (qb_pos==0 + 整包 + 非 master + ≥32KB)
- 双缓冲顺序一致性 (链表出现后静态退休)

### 维度2 性能
- Greedy 多读 vs 精确读 (大参数) 双策略
- writev 批量 (静态+链表拼 iov, ≤64KB/轮)
- 大参数零拷贝 (32KB+ 免复制)
- 从库共享 replBufBlock (零复制)

### 维度3 内存
- 16KB 静态 buf + 16KB 节点 (usable 吃碎片)
- querybuf 滑窗 + 立即 trim (常驻小)
- 超限断连 (防慢客户端吃内存)

### 维度4 一致性
- io 线程只解析不执行 (PENDING_COMMAND)
- master querybuf 双角色 (命令+复制流, repl_applied trim)
- freeClient 释放链顺序 (角色先行)
- push 消息暂存 (MULTI/EXEC 顺序)

### 维度5 负面空间 (已写入两篇各 5 条)
- 不做流式解析 / 不做协议协商 / 不做未认证队列 / 不做零拷贝常态 / 不做错误重试 / 不做输出压缩 / 不做背压调度 / 不做从库私有缓冲

## 结论
R-28 全部锚点行号 116 处验证, 8 闭环完成, 无真实偏差。推断显式标注。待二次 REVIEW + harness。

---

# 二次深度 REVIEW (2026-08-13, 07 五维度轮换 + 内容深度)

## R1 维度1: 叙事桥 + 结构完整性

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 5 | 通过项 | 前置 R-2/R-4/R-1 (序号 < 14) 已讲 ✅; 01→02 承接 (回复怎么组装) ✅; 五结构元素齐备 ✅ | 记录 |

## R2 维度2: 锚点密度

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 6 | 通过项 | 01 篇 ~28 锚点 / 02 篇 ~24 锚点 (🔴A 标准 ≥8) ⏫ | 记录 |

## R3 维度3: 前向引用

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 7 | **前向依赖违规** | 02 前置声明 [[R-9-replication]] — R-9 执行序第 25 位未讲 (07 维度3: 依赖必须序号 < 当前域) — 与 R-21 的 R-15 同类 | 移到对照位, 标注 "仅引用 replBufBlock 概念, 域详述在 R-9" |

## R4 维度4: 横切关注点覆盖

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 8 | 通过项 | 复制 (replBufBlock/repl_applied/replicationCacheMaster) / 认证 (未认证限流) / 脚本 (SCRIPT 拒+yielding) / 阻塞 (BLOCKED 断点+unblock) / watch/pubsub (freeClient 清理) / 内存统计 (updateClientMemUsageAndBucket) — 全覆盖 ✅ | 记录 |

## R5 维度5: 负面空间 + 开篇质量

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 9 | 通过项 | 两篇负面空间各 5 条 ✅; 开篇场景化 ("GET 的字节流旅程/慢客户端怎么被断开/从库为什么没有私有缓冲") ✅ | 记录 |

## R6 内容深度: 反写测试 + 机制逐句对源码

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 10 | **表述错误** | 02 节 2 "prepareClientToWrite 六拒" — 实为 **5 拒 + 1 挂队列** (第 6 分支不是拒绝而是排队) | 大纲/闭环/速查/REVIEW 全量 "六拒"→"五拒" |
| 11 | 通过项 | 其余 ~45 句机制描述逐句对源码一致 ✅ (readlen 四决策/四提前退出/argv 1024 起步 2× 增长/未认证三级限/零拷贝四前提/双缓冲切换/尾节点续写/从库 replBufBlock/sentlen 部分写/释放链顺序/acceptCommonHandler maxclients 检查 L1340) | 记录 |

## 二次 REVIEW 汇总

07 五维度轮换 + 内容深度共 **2 处新发现** (前向依赖违规 1 / 表述错误 1), 全部修复。反写测试结论: 两篇大纲机制面完整可支撑写作。
