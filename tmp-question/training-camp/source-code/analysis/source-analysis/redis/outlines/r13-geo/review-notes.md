# R-13 GEO — 深审 REVIEW 记录 (六层深审 + 07 五维度)

## 六层深审 (Pass 1-3 全程)

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 1 | 预判验证 | 交接文档 §四 三项预判全验证: **GEO_STEP_MAX=26 (52bit) L46** ✅ / **GEOADD 底层是 ZADD** ✅ (geo.c:501-502 replaceClientCommandVector) / **haversine+6372797.560856** ✅ (geohash_helper.c:229-242,52) | 记录 |
| 2 | 机制洞察 | **内部奇偶位与标准 geohash 相反**: interleave64(x=lat) → lat 偶数位/lon 奇数位 (geohash.c:52-77, 调用 L149) — 标准 geohash 是 lon 偶数位 (标准知识 — 推断标注); 自洽无碍 (GEOHASH 输出重编码) | 大纲节 1 + q1 |
| 3 | 精确化 | 详案 "base32 11 字符" → 精确: 11×5=55bit 槽 > 52bit → **第 11 字符 idx=0 恒 '0'**, bits 低 2 位 (bit0-1) 不入字符 (geo.c:920-927) | 大纲节 5 + q5 |
| 4 | 补充锚点 | 邻居移动位运算 geohash_move_x/y (geohash.c:228-264, 掩码 zz 加减进位) / GZERO 宏 (geohash_helper.h:37) / geoArray 8 起步倍增 (geo.c:67-69) | 大纲节 3 补注 |
| 5 | 边界验证 | geohashEncode 校验 step>32/step==0/越界 (geohash.c:125-131); 负半径/负宽高拒绝 (geo.c:164-167,191-194); GEODIST argc>5 syntaxerr (L980-983); COUNT≤0 拒 (L591-594) — GEOADD 固定 26 恒过校验 | 记录 |
| 6 | 行号验证 | 全函数 22 锚点 + 常量 8 锚点 grep 穷举全部命中 (geohash.h:46-52 / geohash.c:52,82,121,164,266 / helper:62,98,121,213,224,229,266 / geo.c:92,261,357,365,445,523,866,870,878,940,973; commands.def:11019-11028; t_zset.c:920,312) | 记录 |

## 07 五维度

### 维度1 功能正确性
- 编码-解码往返: decode 取格中心 (geohash.c:206-215 midpoint), 误差 <0.6m 自洽 (2^26 定点推算)
- 前缀范围 [align(h), align(h+1)) 半开语义 (geo.c:264,328-352)
- 矩形过滤顺序正确性 (先纬后经, 失败早退不误判)

### 维度2 性能
- 两段式: 9 盒粗筛 + haversine 精筛
- v==0 同经度捷径 / 矩形先纬后经 (便宜优先)
- qsort 全量 vs pqsort 部分 (COUNT 场景)
- ANY 时 limit 提前退出 (跨盒累计 ga->used)

### 维度3 内存
- 零额外存储: GEO 数据全在 zset 内 (score 即坐标)
- geoArray 8 起步倍增
- STORE 临时 zset 构建后 setKey

### 维度4 一致性
- 单位换算: 回复距离除以 conversion (geo.c:780); STOREDIST score=距离 (L819)
- GEOHASH 重编码标准 -90/90 兼容
- 缺键分支 (STORE 删键回 0 / 空数组)

### 维度5 负面空间 (已写入大纲 6 条)
- 不做投影转换 / 不做动态降精度 / 不做跨 key 索引 / 不做距离缓存 / 不做删除单元 / 不做覆盖保证

## 结论
R-13 全部锚点行号 ~40 处验证, 6 闭环完成, **机制洞察 1 (奇偶位反向) + 精确化 1 (第 11 字符)**。推断 4 处显式标注。待二次 REVIEW 复核。

---

# 二次深度 REVIEW (2026-08-13, 07 五维度轮换 + 内容深度)

## R1 维度1: 叙事桥 + 结构完整性

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 7 | 通过项 | 前置 R-6/R-12/R-21/R-1 (序号 < 21) 均已讲 ✅; 引出 R-15 (未来域 OK, 槽位散列对照); 对照 R-12/R-11 ✅; 五结构元素齐备 ✅; 读者处境场景化 (附近的人/score 承载/ZRANGE 可查/0.6m) ✅ | 记录 |

## R2 维度2: 锚点密度

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 8 | 通过项 | 大纲 ~40 锚点 (file:line) — 🟡B 标准 ≥4 ⏫ | 记录 |

## R3 维度3: 前向引用

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 9 | 通过项 | 前置声明无未来域 (R-15 仅作引出) ✅ | 记录 |

## R4 维度4: 横切关注点覆盖

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 10 | 通过项 | 传播 (ZADD 委托继承 / STORE dirty) / 通知 (geosearchstore/georadiusstore/del) / 编码 (zset 双编码遍历) / 键空间 (lookupKeyRead/checkType/setKey) / 淘汰 (CMD_DENYOOM) — 全覆盖 ✅ | 记录 |

## R5 维度5: 负面空间 + 开篇质量

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 11 | 通过项 | 负面空间 6 条 ✅; 开篇场景化 ✅ | 记录 |

## R6 内容深度: 反写测试 + 机制逐句对源码

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 12 | 精确化 | 大纲节 4 "矩形过滤顺序 (先纬度 → 后经度 → 最后真实距离)" — 补锚点 geohashGetDistanceIfInRectangle (helper L266-280, 顺序 L270-278); **同经度捷径触发面**: 矩形经度步距在 y2 纬度算 (L274) — 与 v==0 捷径不同层, 表述已分 | 大纲节 4 已含锚点, 记录 |
| 13 | **强断言实证** | 大纲节 2 "CH 透传 (Handle in zaddCommand L455)" — grep 实证 zaddCommand 处理 CH 选项 (t_zset.c 存在 CH 处理逻辑 — 注释 L455 指向) ✅; "NX/XX 互斥" 与 GEOADD L460-464 校验一致 | 记录 |
| 14 | 推断标注核查 | 节 1 精度 (0.6m/0.3m) 与节 3 step 例 (10km→10) — 已标注推断; 奇偶位反向标注标准知识推断 | 记录 |
| 15 | 通过项 | 其余 ~40 句机制描述逐句对源码一致 ✅ (归一定点/交错/包围盒 cos 修正/南半球反转/decrease_step/GZERO 剔除/9 盒去重/haversine/双编码遍历/pqsort/COUNT 强制 ASC/STORE 手工建 zset/重编码 11 字符/单位表/缺键分支) | 记录 |

## 二次 REVIEW 汇总

07 五维度轮换 + 内容深度共 **2 处修正** (补充锚点 1 / 强断言实证 1), 全部验证。反写测试结论: 大纲机制面完整可支撑写作。

---

# 三次深度 REVIEW (2026-08-13, 逐句对源码 + 推理验证)

> 动机: 用户要求深度 REVIEW — 对大纲每个锚点重新 grep, 对全部推断/手算数字反推验证。

## 推理验证 (全部反推通过)

| # | 项 | 验证过程 | 结论 |
|:--:|:--|:--|:--:|
| V1 | 10km → step 10 | 手算: 10000×2^k ≥ 20037726.37 → k=11 (20480000) → step=1+11=12 → 12-2=10; 高纬修正仅 |lat|>66 触发, 10km 于低纬 → **step 10** ✅ | 通过 |
| V2 | 精度 0.6m/0.3m | 360/2^26=5.364e-6° × 111320m/° ≈ 0.597m; 170.1/2^26=2.535e-6° × 111132 ≈ 0.282m ✅ | 通过 |
| V3 | 11 字符取位 | i=0..9: bits>>(52-(i+1)*5): i=0→>>47 (取 bit51-47 最高组), i=9→>>2 (取 bit6-2) — **bit1/bit0 不入字符**, i=10 恒 idx=0 ✅ | 通过 |
| V4 | 奇偶位 | interleave64(lat,lon)=lat\|(lon<<1) → lat bit0,2,4 (偶数) ✅; deinterleave 解码 L178-179 ilato=低 32 位=lat 一致 ✅ | 通过 |
| V5 | haversine | a = u² + cos(lat1)·cos(lat2)·v² (helper L240) ✅; v==0 捷径 L235-236 ✅ | 通过 |
| V6 | [min,max) 半开 | zrangespec minex=0/maxex=1 (geo.c:264) ✅; scoresOfGeoHashBox max 排除 (L350-351) ✅ | 通过 |

## 新发现问题 (4 处, 全部修复)

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 16 | **语义精确化** | 节 4 "COUNT 无排序 → 强制 ASC" **漏 `!any` 条件** — 实测 L718 `if (count != 0 && sort == SORT_NONE && !any)`; 注释 L717 明示 "not needed for ANY option"; ANY 时**不**强制排序 | 大纲节 4 修正为 "COUNT 无排序且非 ANY → 强制 ASC" |
| 17 | 负面空间精确化 | 第 6 条 "靠精筛兜底" 表述误导 — **精筛只过滤不补漏**: 9 盒 + decrease_step 是近似覆盖, 边界格成员可能漏检 | 大纲负面空间第 6 条重写 |
| 18 | **命名语义澄清** | geohash_move_x/y (geohash.c:228-264): 局部变量 x = bits&0xaaaa (**奇数位=lon**), y = bits&0x5555 (偶数位=lat) — 与 interleave64 参数名 xlo=lat 相反! 行为正确 (east/west 移经度, zz+1 加法进位桥传播), 但跨函数命名轴语义不同 — 记录防读者误解 | review-notes 记录 |
| 19 | 外部知识标注 | "去 a/i/l/o" (base32 字母表 L879) — 代码无注释, 标准 geohash 约定 — 补推断标注 | 大纲节 5 补 "推断标注" |

## 三次 REVIEW 汇总

推理验证 6 项全通过 (V1-V6); 新发现 **4 处** (语义精确化 1 / 负面空间 1 / 命名澄清 1 / 标注 1), 全部修复。锚点逐句 re-grep 无行号偏移。大纲现可支撑写作。
