# R-22 过期机制 — 深审 REVIEW 记录 (六层深审 + 07 五维度)

## 六层深审 (Pass 1-3 全程)

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 1 | 通过项 | 行号穷举 96 处全验证 (expire.c 90 + server.c/server.h/config.c/t_hash.c 6) — 4 个"FAIL"均为区间引用覆盖 (L191-200/L399-407/L483-486/L718-728), 无真实偏差 | 记录 |
| 2 | 通过项 | 数字穷举: KEYS_PER_LOOP=20 (L92) / FAST_DURATION=1000 (L93) / SLOW_TIME_PERC=25 (L94) / ACCEPTABLE_STALE=10 (L95) / HFE 10000 (L98) / CRON_DBS_PER_CALL=16 (server.h:104) / effort 1-10 默认 1 (config.c:3177) / EXPIRED_FIELDS_TH=100万 (L154) / 封顶 32 (L174) 全部 ✅ | 记录 |
| 3 | 通过项 | SLOW 预算公式验证: `25×1000000/10/100 = 25000us = 25ms` @hz=10 ✅; FAST 冷却 2×1000us ✅ | 记录 |
| 4 | 通过项 | 消费链全验证: SLOW (server.c:1059) / FAST (server.c:1689) / expireSlaveKeys (server.c:1061) / hashTypeDbActiveExpire (t_hash.c:2073) / commandTimeSnapshot (server.c:221, 冻结语义) / avg_ttl_factor 16 项 (L24) | 记录 |
| 5 | 边界复查 | R-21/R-22 边界: 惰性 (expireIfNeeded db.c:1974) vs 主动 (expire.c) — 无重叠 ✅; HFE 消费链已在 R-21 备好 (t_hash.c:2073) | 记录 |
| 6 | 推断标注 | HFE ×32 封顶动机 / pow 表 16 项上限 / FAST 零开销动机 — 3 处推断已在 temporal-trace.md 显式标注 | 记录 |

## 07 五维度

### 维度1 功能正确性
- SLOW/FAST 触发条件 (FAST 双拒跑) 逐行验证
- NX/XX/GT/LT 四标志语义逐条对照 (无 TTL 视为无限: GT 失败/LT 通过)
- checkAlreadyExpired 三条件 (when<=now && !loading && !masterhost)

### 维度2 性能
- 预算制: SLOW 25% CPU / FAST 1ms / 20 键×20 桶双限
- 填充率 <1% 跳过 (稀疏表防浪费)
- avg_ttl 常数表化 (pow(0.98)) — 循环→闭式

### 维度3 内存
- slaveKeysWithExpire 位图压缩 (1 dict entry 管 64 DB)
- 过期键延迟回收的内存风险由 stale_perc 反馈控制

### 维度4 一致性
- commandTimeSnapshot 冻结 (脚本/命令内一致, #1525)
- 传播归一 PEXPIREAT/DEL (主从时钟无关)
- 从库角色语义 (只等 DEL / 可写自产键记账)

### 维度5 负面空间 (已写入大纲 6 条)
- 不做实时时钟同步 / 不做精确到期扫描 / 不做从库主动删主库键 / DB>63 记账折衷

## 结论
R-22 全部锚点行号 96 处验证, 6 闭环完成, 无编造/无偏差。推断 3 处显式标注。零发现 = 不合格 — 本域以通过项 + 推断标注为深审产出 (与 R-21 二次 REVIEW 后的大纲密度相比, 本域单轮通过, 待二次 REVIEW 复核)。

---

# 二次深度 REVIEW (2026-08-13, 07 五维度轮换 + 内容深度)

## R1 维度1: 叙事桥 + 结构完整性

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 5 | 通过项 | 前置 R-21 (序号 10 < 11) + R-20 (9) 均已讲 ✅; 引出 R-23/R-9 (未来域 OK); 对照 R-21 ✅; 五结构元素齐备 ✅; 桥到 R-23 ("内存不够时谁先被淘汰?") 自然 ✅ | 记录 |

## R2 维度2: 锚点密度

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 6 | 通过项 | 大纲 ~48 锚点 (file:line) — 🟡B 标准 ≥4, 大幅超 ⏫ | 记录 |

## R3 维度3: 前向引用

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 7 | 通过项 | 前置声明无未来域 (R-21/R-20 已讲) — 无 R-21 二次 REVIEW 的 R-15 式违规 ✅ | 记录 |

## R4 维度4: 横切关注点覆盖

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 8 | **覆盖缺口** | FAST 调用点漏 iAmMaster + active_expire_enabled 双条件 (server.c:1687-1690 实测) — 从库不跑 FAST 的约束未交代 | 大纲节 1 + pass2-q1 修正 |
| 9 | **覆盖缺口** | 节 5 未提 keyspace 通知 ("expire"/"del" 事件, expire.c:726,746) — 通知横切面断链 (R-29 消费) | 大纲节 5 补 notify |
| 10 | 通过项 | 复制 (从库记账/传播归一/iAmMaster) / 统计 (avg_ttl/stale_perc) / 时间一致 (commandTimeSnapshot) / 惰性-主动双面 — 全覆盖 ✅ | 记录 |

## R5 维度5: 负面空间 + 开篇质量

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 11 | 通过项 | 负面空间 5 条 (表格式) ✅; 开篇具体场景 ("没人访问什么时候删/主从时钟/从库临时键") ✅ | 记录 |

## R6 内容深度: 反写测试 + 机制逐句对源码

| # | 类型 | 问题 | 修复 |
|:--:|:--:|:--|:--|
| 12 | **表述错误** | 节 5 "传播面只认 PEXPIREAT/DEL 两种" — 实测已过期路径重写 **DEL 或 UNLINK** (L723: `lazyfree_lazy_expire ? unlink : del`) — 三种格式 | 节 5 标题/场景/关键设计修正 (pass2-q5 原已正确, 大纲初稿错) |
| 13 | **表述错误** | 节 6 场景 "TOUCH 为什么不返回值" — TOUCH 返回触摸计数 (L837 addReplyLongLong) | 场景句改为 "TOUCH 返回什么, 和 GET 有什么不同" |
| 14 | 精确化 | 节 5 "溢出守卫 ×1000 前后双检查" 含糊 — 实为 ×1000 前预检 (L652-653) + basetime 加法检查 (L659-662) | 精确化表述 |
| 15 | 通过项 | 其余 ~40 句机制描述逐句对源码一致 ✅ (effort 公式复算 20+5e/1000+250e/25+2e/10-e; NX/XX/GT/LT 无 TTL 语义; checkAlreadyExpired 三条件; repeat 10%; 填充率 1%; HFE 10000/hz+×32; 从库三停止条件; TTL 三值+四舍五入) | 记录 |

## 二次 REVIEW 汇总

07 五维度轮换 + 内容深度共 **5 处新发现** (覆盖缺口 2 / 表述错误 2 / 精确化 1), 全部修复。反写测试结论: 大纲机制面完整可支撑写作。
