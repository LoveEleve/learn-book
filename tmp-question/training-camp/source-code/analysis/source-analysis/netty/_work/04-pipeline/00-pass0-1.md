# Pipeline+Handler — Pass 0-1

> 域: Pipeline | 方案: A (全量深挖) | 包: io.netty.channel | 预估: 3 篇

## Pass 0: 预读

| 数据源 | 结果 |
|---|---|
| 设计文档 | 0 个 |
| git log | shallow clone |
| 测试文件 | DefaultChannelPipelineTest, AbstractChannelHandlerContextTest |

## Pass 1: 扫描轮廓

### 继承树

```
ChannelHandler (interface)
├── ChannelInboundHandler — channelRead/channelActive/channelRegistered 等
└── ChannelOutboundHandler — write/flush/bind/connect/close 等

ChannelHandlerContext (interface — AttributeMap+ChannelInboundInvoker+ChannelOutboundInvoker)
└── AbstractChannelHandlerContext (abstract)
    ├── volatile next (line 63) / volatile prev (line 64) — 双向链表
    ├── fireChannelRead → findContextInbound(MASK) → invokeHandler
    ├── write → findContextOutbound(MASK) → invokeHandler
    └── DefaultChannelHandlerContext (package-private)

DefaultChannelPipeline (implements ChannelPipeline)
├── HeadContext (next=... , prev=null) — Inbound 起点 + Outbound 终点(unsafe.write)
└── TailContext (next=null, prev=...) — Inbound 终点(release unhandled msg)

ChannelHandlerMask — 运行时检测: MASK_ONLY_INBOUND / MASK_ONLY_OUTBOUND 位定义
```

### 基本元素分解

| # | 元素 | 说明 | 源文件 |
|:--:|------|------|--------|
| 1 | **ChannelHandler** | 生命周期: handlerAdded/handlerRemoved | `ChannelHandler.java:178` |
| 2 | **ChannelInboundHandler** | Inbound: channelRead, channelActive, exceptionCaught 等 9 方法 | `ChannelInboundHandler.java:22` |
| 3 | **ChannelOutboundHandler** | Outbound: write, flush, bind, connect, close 等 | `ChannelOutboundHandler.java:23` |
| 4 | **AbstractChannelHandlerContext** | 双向链表: volatile next/prev + fireChannelRead/write 传播 | `AbstractChannelHandlerContext.java:60-64` |
| 5 | **DefaultChannelPipeline** | Head+Tail 端点 + addLast/addFirst/remove | `DefaultChannelPipeline.java:45-97` |
| 6 | **HeadContext** | Inbound 起点(fireChannelRead) + Outbound 终点(unsafe.write) | `DefaultChannelPipeline.java:1324-1437` |
| 7 | **TailContext** | Inbound 终点: onUnhandledInboundMessage→release | `DefaultChannelPipeline.java:1264-1321` |
| 8 | **ChannelHandlerMask** | 编译期检测: MASK_ONLY_INBOUND/MASK_ONLY_OUTBOUND 位掩码 | `ChannelHandlerMask.java:44-63` |

### Flagged Questions (7)

1. **Q1**: 双向链表结构 — volatile next/prev + addLast 插入时的指针更新顺序？双向链表如何保证线程安全？

2. **Q2**: Inbound 传播 — fireChannelRead → findContextInbound(MASK) → skipContext 掩码机制？ChannelHandlerMask 如何编译期检测 handler 覆盖了哪些方法？

3. **Q3**: Outbound 传播 — write/flush 的 findContextOutbound 反向遍历 + HeadContext 的 unsafe.write 终止？

4. **Q4**: ChannelHandlerMask 检测 — isSkippable 原理？MASK_ONLY_INBOUND 包含哪些位？mask=0 的 handler (Adapter) 被跳过的最佳路径？

5. **Q5**: TailContext 自动释放 — ReferenceCountUtil.release(msg) 兜底机制（ByteBuf 篇一中已提到，此处展开实现层）

6. **Q6**: HeadContext I/O 触发 — readIfIsAutoRead + channelRead 后触发 read() 形成 read loop？

7. **Q7**: Pipeline 动态修改 — addLast/addFirst/remove 的线程安全机制

## 方法论证据

```
[00 Pass 0-1] 继承树: 2层, 元素: 8, 标记问题: 7
```
