# ByteBuf — Pass 0-1

> 域: ByteBuf | 方案: A (全量深挖) | 包: io.netty.buffer
> 方法: 方法论/01 Pass 0-1

---

## Pass 0: 预读

| 数据源 | 结果 |
|---|---|
| 设计文档 | 0 个(仓库内无 docs/) |
| CHANGELOG/RELEASE | 0 个 |
| git log | 1 个(grafted shallow clone) |
| 测试文件 | 10+ 个(AbstractByteBuf/AbstractPooledByteBuf/CompositeByteBuf/AdaptiveByteBuf 等) |

---

## Pass 1: 扫描轮廓

### 包结构 (`buffer/src/main/java/io/netty/buffer/`)

```
io/netty/buffer/
├── search/          (文本搜索子包)
└── 83 Java 文件      (39 个公共类/接口)
```

### 继承树

```
ReferenceCounted                            ← 引用计数接口
  └── ByteBuf (abstract, 2508行)            ← 核心抽象: 双指针 readerIndex/writerIndex
        ├── AbstractByteBuf (abstract, 1507行) ← 通用实现基础
        │     ├── AbstractReferenceCountedByteBuf  ← 引用计数实现
        │     │     ├── CompositeByteBuf           ← 零拷贝组合 (AggregatedBuffer)
        │     │     ├── PooledByteBuf              ← 池化缓冲区基类
        │     │     └── UnpooledHeapByteBuf / UnpooledDirectByteBuf
        │     └── AbstractUnpooledByteBuf
        └── ...

ByteBufAllocator (interface)
  └── AbstractByteBufAllocator (abstract)
        ├── PooledByteBufAllocator          ← 池化分配器
        └── UnpooledByteBufAllocator        ← 非池化分配器
```

### 基本元素分解

| # | 元素 | 说明 | 源文件 |
|:--:|------|------|--------|
| 1 | **ByteBuf** | 核心抽象类，双指针模型 (readerIndex ≤ writerIndex ≤ capacity) | ByteBuf.java:248 |
| 2 | **ReferenceCounted** | 引用计数接口 (refCnt/release/retain) | ByteBuf.java:248 implements |
| 3 | **ByteBufAllocator** | 分配器 SPI (buffer/directBuffer/compositeBuffer/ioBuffer) | ByteBufAllocator.java:22 |
| 4 | **AbstractByteBuf** | 通用实现: leakDetector + maxCapacity + SwappedByteBuf | AbstractByteBuf.java:47 |
| 5 | **CompositeByteBuf** | 零拷贝聚合缓冲区，Iterable<ByteBuf> | CompositeByteBuf.java:49 |
| 6 | **PooledByteBufAllocator** | 池化分配器: PoolArena + PoolChunk Buddy 分配 | PooledByteBufAllocator.java:38 |
| 7 | **Unsafe/Adaptive** | UnpooledDirectByteBuf(直接内存)、UnpooledHeapByteBuf(堆内存)、AdaptivePoolingAllocator(2153行) |

### 测试文件功能地图

| 测试文件 | 覆盖概念 |
|---|---|
| `AbstractByteBufTest` | ByteBuf 基础 API: read/write/mark/discardReadBytes |
| `AbstractReferenceCountedByteBufTest` | 引用计数: retain/release/refCnt |
| `AbstractPooledByteBufTest` | 池化: allocate/release/池大小 |
| `AbstractCompositeByteBufTest` | 组合: addComponent/removeComponent/merge |
| `AdaptiveByteBufAllocatorTest` | 自适应分配: 根据历史用量调整池大小 |

### Flagged Questions (6)

1. **Q1**: ByteBuf 的 readerIndex/writerIndex 双指针设计比 Netty 3 的单一 index 有什么优势？mark/reset/discardReadBytes 的作用？
2. **Q2**: ReferenceCounted 的 retain()/release()/refCnt() 何时调用？refCnt=0 时如何回收内存？和 GC 的关系？
3. **Q3**: CompositeByteBuf 内部用什么数据结构实现零拷贝？addComponent/removeComponent 对底层 ByteBuf 的引用计数有什么影响？
4. **Q4**: PooledByteBufAllocator 如何管理 PoolArena？Buddy 分配算法的 chunk/subpage 机制？LeakDetector 如何检测泄漏？
5. **Q5**: HeapByteBuf vs DirectByteBuf 的内存布局差异？`hasArray()` 什么时候返回 true？unsafe 访问的优化是什么？
6. **Q6**: `slice()`/`duplicate()`/`copy()` 三者在引用计数和数据共享上的语义区别是什么？maxCapacity 的继承规则？

---

## 方法论执行证据

```
[00 Pass 0] 设计文档: 0个, git log: 1个, 测试: 10+个
[00 Pass 1] 包: 1个, 公共类/接口: 39个, 继承树: 2层, 基本元素: 7个, 标记问题: 6个
```
