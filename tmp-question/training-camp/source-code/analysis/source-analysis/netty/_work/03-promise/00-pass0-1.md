# Promise/Future — Pass 0-1

> 域: Promise/Future | 方案: B (深挖) | 包: io.netty.util.concurrent | 预估: 2 篇

## Pass 0: 预读

| 数据源 | 结果 |
|---|---|
| 设计文档 | 0 个 |
| git log | shallow clone |
| 测试文件 | DefaultPromiseTest (核心), ChannelFutureTest 等 |

## Pass 1: 扫描轮廓

### 继承树

```
java.util.concurrent.Future<V>
  └── io.netty.util.concurrent.Future<V>   — addListener/await/sync/isSuccess/cause
        └── Promise<V>                     — writable: setSuccess/setFailure/trySuccess/tryFailure
              └── ChannelPromise            — channel-specific: channel() + isVoid
        └── ChannelFuture                  — channel-specific: channel() + isVoid

AbstractFuture<V> (implements Future)
  └── DefaultPromise<V>                    — core implementation
        └── DefaultChannelPromise          — channel variant

GenericFutureListener<F>                   — listener interface
FutureListener<V>                          — typed convenience
```

### 基本元素分解

| # | 元素 | 说明 | 源文件 |
|:--:|------|------|--------|
| 1 | **Future** | Netty 异步结果: extends JDK Future, addListener/sync | `Future.java:26` |
| 2 | **Promise** | 可写的 Future: setSuccess/setFailure, try* 变体 | `Promise.java:21` |
| 3 | **DefaultPromise** | 核心实现: volatile result + CAS waiters + listener通知 | `DefaultPromise.java:37` |
| 4 | **ChannelFuture/ChannelPromise** | Channel 绑定的变体: isVoid() 避免分配 | `ChannelFuture.java` |
| 5 | **GenericFutureListener** | 监听器: operationComplete() 回调 | `GenericFutureListener.java` |

### 核心实现 (DefaultPromise.java)

- `volatile Object result` (`line 63`) — null=incomplete, SUCCESS/UNCANCELLABLE/CauseHolder/CancellationException
- `setSuccess0(result)` → CAS `RESULT_UPDATER.compareAndSet(this, null, result)` → `notifyListeners()` 通知所有注册的监听器
- `setFailure0(cause)` → CAS → `notifyListeners()`
- `addListener(listener)` → 如果已 complete → immediate notify; 否则 → 加入 listener 列表
- `short waiters` (`line 77`) — 等待线程计数, await() 阻塞的线程数

### Flagged Questions (5)

1. **Q1**: Future 和 Promise 的分离设计 — 为什么分开读接口和写接口？Channel 拿到 Future(只读)，EventLoop 拿到 Promise(可写) — 读写分离防止 Channel 意外修改异步结果？

2. **Q2**: DefaultPromise 的通知机制 — `notifyListeners()` 为什么用 while-loop 而非 for-loop？listener 数组的扩容策略？immediate notify(已complete 时 addListener) 和 deferred notify(setSuccess 时) 的两种路径？

3. **Q3**: await/sync 阻塞语义 — `await()` 的 Object.wait() + notifyAll 实现？`sync()` 与 `await()` 的区别(异常抛出)？`awaitUninterruptibly()` 如何处理中断？

4. **Q4**: ChannelFuture — `isVoid()` 是什么？EventLoop.write() 返回 void promise 避免分配 — Netty 的零分配的极致追求？

5. **Q5**: 异常传播 — setFailure 后的 cause 如何从 EventLoop 传播到调用方？GenericFutureListener 中的 operationComplete 如何拿到失败原因？

## 方法论证据

```
[00 Pass 0-1] 继承树: 2层, 元素: 5, 标记问题: 5
```
