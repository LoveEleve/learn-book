# 内存池化 — Pass 0-1

> 域: Memory Pool | 方案: A (全量深挖) | 包: io.netty.buffer | 预估: 3 篇

## Pass 0: 预读

| 数据源 | 结果 |
|---|---|
| 设计文档 | 0 个 |
| git log | shallow clone |
| 代码规模 | 5 文件 2804 行: PoolArena(853) + PoolChunk(739) + PoolThreadCache(499) + SizeClasses(413) + PoolSubpage(300) |
| 测试文件 | PooledByteBufAllocatorTest, PoolArenaTest, PoolChunkTest |

## Pass 1: 扫描轮廓

### 继承树

```
PooledByteBufAllocator (Ch4 已覆盖)
  ├── PoolArena<T>[] heapArenas / directArenas  ← Arena 数组
  ├── SizeClasses                                   ← 大小归一化表
  └── PoolThreadLocalCache (extends FastThreadLocal) ← 每线程缓存

PoolArena<T> (853行)
  ├── PoolChunkList<T> q000..q100                  ← 6级使用率队列
  ├── PoolSubpage<T>[ ] smallSubpagePools          ← 小分配 subpage 池
  └── allocate(cache, reqCapacity, maxCapacity)     ← 主分配入口

PoolChunk<T> (739行)
  ├── runsAvail: PriorityQueue<Integer>[ ]         ← 同大小 run 按 offset 排序
  ├── runsAvailMap: LongLongHashMap                 ← runOffset → handle 双向索引
  └── handle: 64bit 编码 (runOffset:15 + size:15 + isUsed:1 + isSubpage:1 + bitmapIdx:32)

PoolSubpage<T> (300行)
  ├── bitmap: long[ ]                               ← 每 bit 标记一个元素
  ├── maxNumElems / numAvail / nextAvail / doNotDestroy
  └── allocate() / free()

SizeClasses (413行)
  ├── sizeClasses: short[ ][ ]                     ← sizeIdx × 3 → [log2Group, log2Delta, nDelta]
  ├── sizeIdx2sizeTab: int[ ]                       ← sizeIdx → normalized capacity
  └── size2SizeIdx(int): 二分查找的规范化
```

### 基本元素分解

| # | 元素 | 说明 | 源文件 |
|:--:|------|------|--------|
| 1 | **PoolChunk** | 连续内存块: 默认 4MB, PageRun/PoolSubpage 两级分配, 64bit handle 编码 | `PoolChunk.java:138` |
| 2 | **PoolSubpage** | bitmap 位图分配: 每 bit=一个元素, nextAvail 快速路径 | `PoolSubpage.java:26` |
| 3 | **PoolArena** | Arena: 6级 PoolChunkList + smallSubpagePools, allocate 入口 | `PoolArena.java` |
| 4 | **SizeClasses** | 规范化表: tiny/small/normal/huge, size2SizeIdx 二分查找 | `SizeClasses.java` |
| 5 | **PoolThreadCache** | 每线程缓存: MemoryRegionCache × 3 (tiny/small/normal) | `PoolThreadCache.java` |
| 6 | **PoolChunkList** | 6级使用率队列 (q000=0-25%...q100=100%) — 动态晋升/降级 Chunk | `PoolChunkList.java` |
| 7 | **handle** | 64bit 编码: 完整 offset+size+flags — 一次读取得到全部分配信息 | `PoolChunk.java:76-86` |

### Flagged Questions (7)

1. **Q1**: PoolChunk.handle 的 64bit 编码 — runOffset(15)+size(15)+isUsed+isSubpage+bitmapIdx(32) 如何压缩/解码？alloc/free 如何从 handle 重构完整分配信息？

2. **Q2**: PoolSubpage 的 bitmap 分配 — 64bit long[] 每 bit 标记一个元素，nextAvail 快速路径如何实现？doNotDestroy 缓存策略？

3. **Q3**: PoolArena 的 6 级 PoolChunkList — q000/q025/q050/q075/q100 五级 + PoolChunkListMetric？Chunk 如何在不同使用率队列间晋升/降级？

4. **Q4**: SizeClasses 规范化表 — sizeIdx × 3 的 [log2Group, log2Delta, nDelta] 编码结构？size2SizeIdx 二分查找的 lookup 表设计？

5. **Q5**: PoolThreadCache 的 MemoryRegionCache — 三层缓存 (tiny/small/normal) 各多少条目？alloc/free 的快速路径和 cache 溢出后回 Arena 的机制？

6. **Q6**: AdaptivePoolingAllocator — 如何根据历史分配量调整池大小？与固定 PooledByteBufAllocator 的区别？

7. **Q7**: 6 级 ChunkList 的晋升/降级 — PoolChunk 的 free() 后 `usage()` 改变 → `move()` 或 `add0()` 到对应队列？

## 方法论证据

```
[00 Pass 0-1] 文件: 7, 代码行: 2804, 元素: 7, 标记问题: 7
```
