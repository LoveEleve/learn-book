# 快速参考卡

> 角色：从 methodology/01-05 提取的常用命令速查。分析时快速查阅——不是完整 SOP 的替代品。
> 来源：methodology/zh/01-05

---

## 第零遍：读代码前的上下文

```bash
# 设计文档
Read README.md
find docs/ -name "*.md" | head -5

# Issues/PRs（设计讨论）
git log --oneline -30
git log --oneline --grep="refactor|design|architecture|rewrite"

# Release notes
ls CHANGELOG* RELEASE* 2>/dev/null
# 如果找到，Read 最近的一份

# 测试目录当功能地图
find src/test -type d | sort | head -20
```

**MCP**：GitHub MCP `search_issues` → 无 GitHub 时 `git log --grep="fix|bug|refactor|issue"`

---

## 第一遍：扫轮廓

```bash
cd /data/workspace/source-code/code/spring/{repo}/

# 包结构
find src/main/java -type d | sort | head -30

# 核心类声明
grep -rn "^public (class|interface|enum) " src/main/java/{pkg} | head -20

# 最热文件（git log trick）
git log --pretty=format: --name-only | sort | uniq -c | sort -rn | head -10

# 读 2-3 个测试文件
find src/test/java -name "*Test*.java" | head -5
```

**MCP**（Java）：JavaLens `get_type_hierarchy` / codegraph `callers` + `callees`
**MCP**（C/C++）：LSP MCP `type_hierarchy` / codegraph `callers` + `callees`

### 第一遍产出模板
```
## 第一遍产出：{域名}

### 继承树/调用图
[ASCII art]

### 基本元素分解
1. 元素A: [一句话] — 对应源码 {file:line}
2. 元素B: [一句话] — 对应源码 {file:line}
...

### 标记问题（至少 5 个）
1. [具体问题]
2. ...
```

---

## 第二遍：盯关键点

### 逐问题闭环
```
1. 读源码位置 → 2. 提假设 → 3. 验证（grep 至少 2 层） → 4. 判代码类型 → 5. 下结论
```

### 追踪执行路径
```bash
# 调用链追踪
grep -rn "methodName(" src/main/java/{pkg} | head -20

# 时空溯源（A方案专用，🔴域）
git tag --sort=version:refname | head -20
git checkout <earliest-tag>
git diff <early-tag> <current-tag> -- {core-package}/
```

**MCP**：JavaLens `analyze_data_flow` / codebase-memory-mcp `trace_path` / Git MCP `git_blame`

### 闭环笔记模板
```
## 闭环笔记：{问题}
假设：[一句话]
验证过程：
  - grep {pattern} → 找到 {file:line}，证实/推翻 XXX
  - Read {file:line} → 方法体中 XXX
  - 追踪到 {file:line} → XXX
代码类型：{Glue/Interface/Implementation/Algorithmic}
跨域关联：[如有] {域名} → {关联类型}
结论：[一句话，附源码位置]
```

---

## 第三遍：收尾

### 文章模板
```
# {文章标题}

## 1. 概述
## 2. 核心机制
   ### 2.1 {元素A}
   ### 2.2 {元素B}
   ### 2.3 组合：{A+B的协同}
## 3. 设计权衡
## 4. 面试考点
   ### Q1: {普通问题}
   ### Q2: {陷阱追问}
## 5. 交叉引用
   ← {前一个域}：{一句话解释为什么它是依赖}
   → {后一个域}：{一句话解释谁依赖它}
   另见：{相关域} — {简要描述}
```

**MCP**：JavaLens `get_complexity_metrics` + `find_circular_dependencies` / codebase-memory-mcp `query_graph`

### 极简复现（A方案🔴专用）
```
在 harness/{framework}/ 中写微缩引擎：
  不要并发安全，不要向后兼容，不要配置
  只保留核心控制流（~200-300 行）
```

---

## 工具降级链

```
Java：   JavaLens → codegraph → codebase-memory-mcp → grep
C/C++：  LSP MCP + clangd → codegraph → codebase-memory-mcp → grep
```

---

## 跨域引用格式

```
{域名} 分析 §{章节}
```

发现传播：第一级（微小→嵌入当前域）/ 第二级（重大→追加修正）/ 第三级（无效→标记重分析）。多域影响（≥3）→ `progress/cross-domain/{id}.md`。
