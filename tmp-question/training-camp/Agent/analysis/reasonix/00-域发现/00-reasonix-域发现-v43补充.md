# Reasonix 域发现 v43 补充(续扫第二十九轮:provider 消息规范化)— 2026-08-14

> 承接:v42。本轮:internal/provider/provider.go(1,132)——消息规范化核心,此前未细看。
> 结论:**closeTruncatedJSON(截断 JSON 自动补全)**与 fast path 优化确认——②执行 provider 层的高价值机制。

---

## 一、v43 深化确认(provider 消息规范化)

| 设计 | 位置 | 要点 |
|------|------|------|
| **closeTruncatedJSON** | provider.go:506-563 | **截断 JSON 自动补全**:栈追踪补全括号/字符串状态机(esc 处理)/尾逗号剥除/尾冒号→null/验证失败回退 "{}"——流式截断参数的可恢复化 |
| **fast path 零分配** | :419-447 | tryNormalizeFastPath:健康回合直通(不分配)——**规范化性能优化** |
| **工具调用名回填** | :388-394 | 空工具名从对应结果回填(#4727,UX 改进) |
| **工具结果配对** | :564+ | **按 id 配对**(重排序回按调用序);**空/重复 id 按位置配对**(防 map 合并——无 id 网关按索引流式) |
| **孤儿工具处理** | :367-418 | provider 发送时丢弃(dropOrphanTools);session 加载保留——**两种语义分离** |
| **InterruptedTurnRecovery** | :137-158 | v13 已记(局部推理 LocalOnly) |

---

## 二、关键设计(通用价值)

1. **"截断 JSON 补全"**:流式/截断的工具参数可恢复(补全括号/字符串)——**容错的最大化**(与 Pi 修复截断、Hermes 处理部分 JSON 同族)
2. **"按位置配对的理由"**:无 id 网关按索引流式,map 会合并结果——**配对策略的边界分析**
3. **"fast path"**:健康消息零分配直通——**热点路径性能**

---

## 三、累计覆盖对账

| 轮次 | 层面 | 新增 | 累计 |
|:--:|------|:--:|:--:|
| v1-v42 | — | 102 | 102 |
| v43 | provider 消息规范化 | +0(深化 3 设计) | **102**(深化) |

> 继续:next 轮 provider/openai.go(1,331)/skill.go(1,425)/config/edit.go(2,442)。
