# Hermes 域发现 v32 补充(续扫第二十一轮:coding_context 姿态)— 2026-08-14

> 承接:v31。本轮:600-1,000 区间抽查,发现 agent/coding_context(916)。
> 结论:**编码姿态单一判定点确认**(RuntimeMode 冻结 + 快照进稳定层),深化 ①对齐。

---

## 一、v32 深化确认(coding_context)

| 设计 | 位置 | 要点 |
|------|------|------|
| **姿态单一判定** | coding_context.py:1-30 | **RuntimeMode 冻结 + ContextProfile 注册表(coding/general)**——"整个代码库永不重推导'是否在编码'";每个域读同一解析对象(system prompt 注入/toolset 选择/委派传播/模型/记忆/压缩提示) |
| **快照进稳定层** | :30-35 | 工作区快照**一次性构建进稳定 system-prompt 层,永不每轮重探测**(防碎缓存);分支/脏状态漂移 → brief 让模型用 git 重查 |
| **编码姿态传播** | 委派继承父 toolset——子代理自动继承 | |
| **focus 模式 opt-in** | 默认姿态只改 prompt 不碰工具集(显式启用 image-gen/Spotify 的用户不因在 git 仓库失去它们) | |

**价值**:这是"配置判定单一化"的架构定论——**"每个域读同一解析对象,不各自探测"**(与 Pi resource-loader、Reasonix agentpreset 同思想)。

---

## 二、关键设计(通用价值)

1. **"单一判定点"**:姿态判定一处解析,全库消费——**避免重复推导漂移**
2. **"快照进稳定层"**:一次性构建进缓存稳定层——**prompt 缓存正确性**(与 Hermes 缓存神圣一致)
3. **"默认不碰工具集"**:姿态默认只改 prompt——**最小侵入**

---

## 三、累计覆盖对账

| 轮次 | 层面 | 新增 | 累计 |
|:--:|------|:--:|:--:|
| v1-v31 | — | 81 | 81 |
| v32 | coding_context 姿态 | +0(深化 2 设计) | **81**(深化) |

> 继续:next 轮 context_references(720)/agent/ 剩余小文件/500 以下抽查——按需收尾。
