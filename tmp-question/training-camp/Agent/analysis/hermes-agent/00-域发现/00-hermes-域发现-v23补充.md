# Hermes 域发现 v23 补充(续扫第十二轮:gateway/session 持久化)— 2026-08-14

> 承接:v22。本轮:gateway/session.py(3,966)持久化细节(v11 已记 routing 代际,本轮源码级验证)。
> 结论:代际全序的"双路径共享计数器"与折叠语义确认——④存储一致性。

---

## 一、v23 深化确认(gateway/session 持久化)

| 设计 | 位置 | 要点 |
|------|------|------|
| **双路径共享代际** | :1519-1527 | 全量快照 + 单条快速保存 **必须共享同一计数器**("stale-write 保护 = 序列化时间的全序,两路径独立编号会静默破坏") |
| **折叠语义** | :1538-1560 | 持久化时把 revision 更新的单条**折叠进快照**(revision > generation 的单条覆盖);延迟全量重写不回归新单条 |
| **写入串行化** | :1538 | 所有全量写者过 _save_lock(持久化代际比较) |
| **profile 解析** | :1764 | _resolve_profile_for_key(会话 → profile) |
| **陈旧清理** | :1430 | _prune_stale_sessions_locked |

---

## 二、关键设计(通用价值)

1. **"双路径共享代际"**:快照与增量共用计数器——**写一致性的全序**(v11 记录的代际模式的源码证明)
2. **"折叠不回归"**:单条新于快照 → 折叠覆盖——**延迟写的正确性**

---

## 三、累计覆盖对账

| 轮次 | 层面 | 新增 | 累计 |
|:--:|------|:--:|:--:|
| v1-v22 | — | 80 | 80 |
| v23 | gateway/session 持久化 | +0(源码级验证) | **80**(验证) |

> 继续:next 轮 tts_tool(4,502)/terminal_tool(3,841)/setup.py(3,562)——按需收尾。
