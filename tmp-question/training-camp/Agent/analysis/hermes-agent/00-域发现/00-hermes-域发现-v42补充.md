# Hermes 域发现 v42 补充(续扫第三十一轮:启动安全审计)— 2026-08-14

> 承接:v41。本轮:hermes_cli 150-300 区间抽查(security_audit_startup/prompt_stash/pty_bridge)。
> 结论:**启动安全姿态审计确认**(警告不阻塞),深化 ②安全。

---

## 一、v42 深化确认

### security_audit_startup(285) — 启动安全姿态审计

| 设计 | 位置 | 要点 |
|------|------|------|
| **4 项检查** | :1-20 | root 运行(POSIX uid 0)/SSH 密码认证开启/容器无持久卷(HERMES_HOME 状态易失)/无认证的网关监听器(仪表盘/API server)——**每个独立 fail-safe**(内部错误吞掉无发现);跨平台(POSIX-only 检查 Windows no-op) |
| **动机** | 2026-06 MCP 配置持久化活动(被入侵机器以 root 跑暴露的仪表盘无防火墙,无人告知) | |

### 其余确认

- prompt_stash(272):Ctrl+S 草稿暂存纯状态机(无 prompt_toolkit 导入可单测;**不写盘——草稿常含凭据/NDA/粘贴的秘密,仅内存**)
- pty_bridge(293):仪表盘 chat 的 PTY 桥(POSIX-only,Windows ConPTY 未来增强)

---

## 二、关键设计(通用价值)

1. **"启动安全姿态警告"**:启动时告知"你暴露了"(不阻塞)——**安全信号的主动呈现**
2. **"暂存不写盘"**:草稿含秘密仅内存——**凭据的默认安全**
3. **"fail-safe 检查"**:每个检查独立,错误吞掉——**审计的可靠性**

---

## 三、累计覆盖对账

| 轮次 | 层面 | 新增 | 累计 |
|:--:|------|:--:|:--:|
| v1-v41 | — | 81 | 81 |
| v42 | 启动安全审计 | +0(深化 2 设计) | **81**(深化) |

> **Hermes 续扫 31 轮完成**:gateway//tui_gateway//hermes_cli//cron/ 全部子目录 + 所有行数区间覆盖。剩余:docker/scripts(部署工程)/平台适配器(同构)/i18n(数据)——**程序化收敛达成**。
