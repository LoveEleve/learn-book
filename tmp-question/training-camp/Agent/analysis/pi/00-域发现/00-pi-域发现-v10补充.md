# Pi 域发现 v10 补充(续扫第一轮:auth/工具延迟加载/搜索/server 层)— 2026-08-14

> 触发:Reasonix 深度挖到 101 域后,用户指出"Pi 是否也没探索完毕?"——用同规格(逐目录核对)继续 Pi。
> 本轮:ai/src(auth/utils)、coding-agent/src(cli 子命令/server)、agent/src(search)、server/snapshots。
> 结论:**确认 Pi 也未探索完毕**(88 域后仍有遗漏),本轮 +10 域。

---

## 一、v10 新增域

### 🔴 高价值新增(3 个)

| # | 域 | 文件 | 设计要点 | 产品映射 |
|---|----|------|---------|---------|
| 89 | **凭证存储** | ai/src/auth/credential-store.ts + context.ts + resolve.ts | **InMemoryCredentialStore**:每 provider 一凭证,写按 provider 串行化(promise 链);AuthContext 抽象(env/文件存在,浏览器安全);可注入持久化存储 | 凭证 |
| 90 | **工具延迟加载** | ai/src/utils/deferred-tools.ts | **从 transcript 按使用拆分 immediate/deferred 工具**——与 Hermes tool_search 同构 | ②上下文节省 |
| 91 | **扫描式会话搜索** | agent/src/search/scanning.ts + index.ts | **流式全扫描搜索**:投影器(metadata/entry/label→text)/分页/中止信号;SessionSearchHit(sessionId/seq/snippet)——Pi 的搜索实现形态(非 FTS) | ④检索 |

### 🟡 中价值新增(7 个)

| # | 域 | 文件 | 说明 |
|---|----|------|------|
| 92 | CLI 子命令族 | coding-agent/src/cli/(args 428/auth-check/auth-command/credential-print/list-models) | CLI 启动/认证/模型列表 |
| 93 | 远程模型目录 | core/remote-catalog-provider.ts | **远程目录 4h 刷新**(pi.dev)+ mergeModels(动态覆盖基线) |
| 94 | 代理流函数 | agent/src/proxy.ts | **经 server 路由 LLM 调用**(客户端模式:server 管 auth) |
| 95 | 默认流函数 | agent/src/stream-fn.ts | setDefaultStreamFn 注入(host 提供默认 model runtime,免依赖) |
| 96 | 管理 HTTP | utils/management-http.ts | fetchWithRetry(408/425/429/500/502/503/504);**仅限幂等管理请求**,agent 操作由语义调用者重试 |
| 97 | 服务端快照 | server/snapshots.ts | ServerSnapshotPublisher:revision 递增 + 广播队列串行 |
| 98 | harness 工具辅助 | agent/src/harness/utils/(shell-output 195/truncate 350) | ShellCaptureProgress(截断/全输出路径)+ truncateTail |

---

## 二、跨项目印证(新增)

1. **工具延迟加载三项目**:Pi deferred-tools(transcript 使用拆分)↔ Hermes tool_search(目录分类+预算)↔ Reasonix tool 动态注册——"按需加载工具"是上下文节省的通用模式
2. **搜索三形态**:Pi 扫描式(投影器)↔ Hermes FTS5 降级链 ↔ Reasonix BM25——产品④检索可组合三种
3. **凭证存储**:Pi InMemory(可注入持久化)↔ Hermes secret_scope ↔ Reasonix secrets——凭证抽象通用

---

## 三、累计覆盖对账

| 轮次 | 层面 | 新增 | 累计 |
|:--:|------|:--:|:--:|
| v1-v9 | 原始+复测 | 88 | 88 |
| v10 | auth/工具加载/搜索/server | +10 | **98** |

> 继续:next 轮扫 coding-agent utils 细节、session-backends 迁移、tui 原生层、client 细节。
