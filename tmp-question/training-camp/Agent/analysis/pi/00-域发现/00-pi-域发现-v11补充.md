# Pi 域发现 v11 补充(续扫第二轮:存储/遥测/客户端/协议层)— 2026-08-14

> 承接:v10。本轮:session-backends repo/migrations、client 全树、server transports、protocol、telemetry 实现、ai providers。
> 结论:Pi 88 → 108 域(两轮续扫 +20)。

---

## 一、v11 新增域

### 🟡 中价值新增(6 个)

| # | 域 | 文件 | 设计要点 |
|---|----|------|---------|
| 99 | **SQLite repo 集成** | sqlite/repo.ts(953) | **writer lease 集成**:claimWriterLease(写前租约)/activeWriterError/lostWriterError(租约失败的语义错误区分——"他人活跃"vs"租约丢失");assertUnusedId/requireSessionRow/claimStorage;branch cache 重建 |
| 100 | **存储迁移** | sqlite/migrations/001_initial.sql | schema 全貌:sessions/entries(PRIMARY KEY (session_id,id) + UNIQUE (session_id,seq))/session_sequences/session_stats |
| 101 | **客户端状态管理** | client/src/state.ts + promise.ts + session-handle.ts | ClientState(快照监听器/会话附件/重置);PromiseResolvers;SessionHandle |
| 102 | **遥测抽象** | telemetry/src/(index 357/memory 219/noop) | TelemetryContext/Span 抽象;**敏感属性元数据**(sensitive/cardinality);内存记录器(span 树/id/parent) |
| 103 | **评测 A/B 汇总** | evals/src/vitest-evals/(summary 438/harness-table 193/artifacts 113/reporter 111) | **HarnessObservation 5 态**(scored/unscored/skipped/pending/errored);baseline/candidates/repetition;PairedMetricSummary |
| 104 | **测试 provider** | ai/src/providers/faux.ts(708) | 确定性 mock provider(模型/流/延迟)——测试基础设施 |

### 🟢 工具族(补入,低价值)

- ai/src/utils/(event-stream 225/abort-signals/abort/sanitize-unicode/headers/uuid/hash/pi-user-agent/typebox-helpers/node-http-proxy/provider-env/text/validation)——EventStream 异步迭代类等通用工具

---

## 二、跨项目印证(新增)

1. **writer lease 错误语义**:Pi activeWriterError vs lostWriterError(两种不同的租约失败)——与 Hermes compression_lock holder 检查同思想
2. **遥测敏感属性元数据**:Pi TelemetryAttributeMetadata(sensitive)↔ Hermes monitoring(redaction)↔ Reasonix secrets——**遥测必须声明哪些属性敏感**
3. **评测 5 态**:Pi HarnessObservation(scored/unscored/skipped/pending/errored)↔ Hermes evals/readtool ↔ Reasonix e2ebench——评测结果状态机通用

---

## 三、累计覆盖对账

| 轮次 | 层面 | 新增 | 累计 |
|:--:|------|:--:|:--:|
| v1-v9 | 原始+复测 | 88 | 88 |
| v10 | auth/工具加载/搜索/server | +10 | 98 |
| v11 | 存储/遥测/客户端/协议 | +10 | **108** |

> 剩余:tui 原生层(keys/terminal 系统编程,产品不需要)、providers 各实现(同构)、generated 文件——按产品价值已到边际。
> **Pi 三项目对比定论**:Pi 88→108(续扫证实未探索完毕)、Reasonix 41→101、Hermes 27→80——**"探索完毕"永远需要体量排序 + 逐目录核对双重验证,单靠感觉不可信(#24/D18)**。
