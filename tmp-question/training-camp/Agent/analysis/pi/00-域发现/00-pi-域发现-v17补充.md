# Pi 域发现 v17 补充(续扫第八轮:server 连接/协议/工具脚本)— 2026-08-14

> 承接:v16。本轮:server/(connection/protocol/errors/listener/unix)、scripts 工具。
> 结论:server 层确认(字节连接 + 握手阶段状态机),scripts 是工程工具(不列为域)。

---

## 一、v17 新增/确认

| # | 域 | 文件 | 设计要点 |
|---|----|------|---------|
| 120 | **字节连接抽象** | server/src/connection.ts | **ordered byte connection**:ByteConnection(send/close)/ByteConnectionHandler(onData/onClose/onError)/ConnectionState(握手阶段状态机:awaitingHello→handshaking→ready→closing→closed;sessionIds 集);握手完成标记 |
| 121 | **服务器协议** | server/src/protocol.ts(382) | 协议层(消息编解码 + 阶段推进) |
| 122 | **Unix 传输** | server/src/transports/unix/(listener 434) | Unix socket 监听器——系统编程面 |
| — | scripts/ | (session-transcripts/cost/stats/tool-stats/release 族) | 工程工具(转录提取/成本统计/发布管线)——**不列为域,按工具处理** |

---

## 二、跨项目印证

1. **连接状态机**:Pi ConnectionStage(5 阶段)↔ Hermes stream_single_writer/连接管理 ↔ Reasonix extension rpcwire——**"连接生命周期状态机"是通信层通用模式**
2. **握手/授权分离**:Pi handshakeComplete(握手后才 ready)↔ server auth——"先握手后业务"通用

---

## 三、累计覆盖对账

| 轮次 | 层面 | 新增 | 累计 |
|:--:|------|:--:|:--:|
| v1-v16 | — | 123 | 123 |
| v17 | server 连接/协议层 | +3 | **126** |

> 剩余:providers 各实现(同构)、interactive components(视觉)、generated 文件、部分 utils——继续按需。
