# Pi 域发现 v23 补充(续扫第十四轮:main 入口/compaction 切点细节)— 2026-08-14

> 承接:v22。本轮:main.ts 入口流程、compaction 两层对比细节(findCutPoint/摘要提示词)。
> 结论:findCutPoint 的切点算法与摘要防跑偏提示词确认(q3 深化的实现细节);无新域。

---

## 一、v23 深化确认(compaction 实现细节)

| 设计 | 位置 | 要点 |
|------|------|------|
| **切点算法 findCutPoint** | harness/compaction/compaction.ts:374-431 | 向后累计消息 token 到 keepRecentTokens → 选最近的**合法切点**(findValidCutPoints)→ 回退到 compaction/message 边界(跳过非消息)→ 判定 isSplitTurn(切点非 user 消息且找到回合起点) |
| **摘要防跑偏提示词** | SUMMARIZATION_SYSTEM_PROMPT | **"Do NOT continue the conversation. Do NOT respond to any questions. ONLY output the structured summary"** + 精确格式(Goal/Constraints/Progress(Done)/...) |
| **main 入口流程** | coding-agent/src/main.ts | 参数解析 → 认证检查(auth-check)→ 信任上下文(project-trust)→ 会话选择(session-picker)→ 运行时创建(agent-session-runtime)→ 模式分发(interactive/print/rpc) |

---

## 二、关键设计(通用价值)

1. **切点算法的正确性**:合法切点集合 + token 预算反向扫描 + 边界回退(compaction/message)——**压缩边界的确定性算法**(与 Hermes _align_boundary_backward、Reasonix 边界干净同族)
2. **摘要提示词的防跑偏**:显式"不要继续对话/不要回答问题/只输出结构化摘要"——**摘要器约束的提示词工程**(与 Hermes 压缩 preamble 同思想)
3. **入口流程**:认证检查先于一切(与 Hermes boot 凭证保护同思想)

---

## 三、累计覆盖对账

| 轮次 | 层面 | 新增 | 累计 |
|:--:|------|:--:|:--:|
| v1-v22 | — | 127 | 127 |
| v23 | main 入口/compaction 细节 | +0(深化) | **127**(深化) |

> 剩余实现面:agent-session 内部状态机(已 q1 深挖)/compaction 剩余/utils 工具——Pi 源码实现已到边际。
