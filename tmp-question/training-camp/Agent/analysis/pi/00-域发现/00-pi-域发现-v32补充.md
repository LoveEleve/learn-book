# Pi 域发现 v32 补充(续扫第二十三轮:agent-session 状态机源码验证)— 2026-08-14

> 承接:v31。本轮:agent-session.ts(3,344)状态机源码验证——q1 笔记(13 设计)的源码级确认。
> 结论:q1 笔记正确;发现 1 个新细节(压缩中拒绝提交 prompt),无新域。

---

## 一、v32 验证确认(agent-session 状态机)

| 设计 | 位置 | q1 验证 |
|------|------|---------|
| 双消息队列 | :320-322 _steeringMessages/_followUpMessages | ✅ |
| 双压缩控制器 | :327-328 _compactionAbortController/_autoCompactionAbortController | ✅ |
| 重试控制器 | :335-336 _retryAbortController/_retryAttempt | ✅ |
| bash 控制器集 | :339 _bashAbortControllers(Set) | ✅ |
| **扩展命令优先** | prompt():1116-1130 | 新增细节:/ 开头先试扩展命令(**流式期间也可执行**) |
| **压缩中拒绝提交** | prompt():1132-1136 | 新增细节:"Cannot submit a prompt while compaction is in progress" |
| **扩展 input 拦截** | prompt():1142+ | 技能/模板展开前先 emitInput 拦截 |
| 状态字段全集 | :310-369 | scopedModels/工具注册表/提示词片段/扩展上下文 |

---

## 二、关键设计(通用价值)

1. **"压缩中拒绝提交"**:压缩进行中提交 prompt → 明确错误——**状态机非法迁移的拒绝**(与 Pi applyMutation、Reasonix mutationBarrier 同哲学)
2. **"扩展命令流式期间可执行"**:/ 命令即时执行不排队——**命令优先级**
3. **"input 拦截先于展开"**:扩展拦截在技能/模板展开前——**拦截点优先级**

---

## 三、累计覆盖对账

| 轮次 | 层面 | 新增 | 累计 |
|:--:|------|:--:|:--:|
| v1-v31 | — | 127 | 127 |
| v32 | agent-session 状态机验证 | +0(q1 验证+2 细节) | **127**(q1 源码级验证) |

> **Pi 源码实现面全部覆盖完成**:agent-session(本轮)/harness session(29)/harness 主体(30)/工具两套(31)/settings(25)/resource-loader(26)/model(27)/composer(28)/sdk(24)/compaction(23)/main(23)/server(17)/tui 原生(16)/agent 门面+循环(15)。
> **Pi 深挖 23 轮收官**:127 域 + 40+ 契约深化,源码/测试/实现层三清单全部通过。剩余仅排除清单(视觉/生成物/同构 providers)。
