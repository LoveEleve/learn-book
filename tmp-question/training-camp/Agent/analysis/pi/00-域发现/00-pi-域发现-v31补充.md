# Pi 域发现 v31 补充(续扫第二十二轮:两套工具系统对比 — Q1 闭环)— 2026-08-14

> 承接:v30。本轮验证域发现 v1 的 Q1(两套工具系统关系)——v1 提出后 30 轮才回答。
> 结论:**Q1 已答**:core 是 harness 的超集(编码专用增强),同一 AgentTool 契约不同实现。

---

## 一、Q1 回答:两套工具系统的关系(已验证)

```
harness/tools/(AgentHarnessTool — 通用内核)
  read.ts(144)/bash.ts(161)/edit-diff.ts(500)/edit.ts(127)/write.ts/image.ts/path-utils.ts/tool-context.ts
      ↑ 基础能力:TypeBox schema + 截断(truncateHead) + 图像检测/编码 + 路径解析

coding-agent/tools/(AgentTool — 编码增强,harness 超集)
  read.ts(358)/bash.ts(510)/edit.ts(443)/write.ts(274)/grep.ts(390)/find.ts(380)/ls.ts(230)
      ↑ 编码专用增强:
      - 高亮/渲染(keyHint/keyText/theme.highlightCode)
      - 图像处理(processImage/image-process)
      - 路径解析增强(resolveReadPathAsync/resolveToCwd)
      - ToolDefinition 包装(wrapToolDefinition — 扩展契约桥)
      - 实验采样(getExperimentalToolSampling)
      - shell 配置(stdin 命令传输/会话环境注入/shellPath)
      - 预览行数 BASH_PREVIEW_LINES + 更新节流 BASH_UPDATE_THROTTLE_MS
```

**关系**:core 工具 = harness 工具 + 编码专用增强,共享同一 AgentTool 契约;bash 从 161 → 510 行展示增强幅度。

---

## 二、关键设计(通用价值)

1. **"通用内核 + 域增强"**:harness(通用)→ core(编码)——**工具分层的架构**(与 Hermes harness/core 工具分层、Reasonix tool/builtin 同思想)
2. **"同一契约不同实现"**:AgentTool 契约统一——**可替换性**
3. **"stdin 命令传输"**:bash 支持 stdin 传输命令(防参数过长/命令行注入)——**命令传输安全**

---

## 三、累计覆盖对账

| 轮次 | 层面 | 新增 | 累计 |
|:--:|------|:--:|:--:|
| v1-v30 | — | 127 | 127 |
| v31 | 两套工具系统对比 | +0(Q1 闭环) | **127**(Q1 闭环) |

> v1 的 Q1 遗留 30 轮后闭环。剩余 Q2-Q10 中 Q2(两套 compaction 关系)在 v23 部分回答,其余 q 笔记已覆盖。
> 剩余实现面:agent-session 状态机内部(已 q1)——Pi 源码实现面仅剩此。
