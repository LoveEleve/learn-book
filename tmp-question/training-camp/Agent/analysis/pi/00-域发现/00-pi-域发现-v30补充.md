# Pi 域发现 v30 补充(续扫第二十一轮:agent-harness 主体)— 2026-08-14

> 承接:v29。本轮:agent-harness.ts(508)主体——之前只提过没细看。
> 结论:AgentLane 完整契约(18+ 操作)与错误分类学(14 个 TaggedError)确认,深化②执行引擎。

---

## 一、v30 深化确认(agent-harness 主体)

| 设计 | 位置 | 要点 |
|------|------|------|
| **AgentLane 契约全集** | agent-harness.ts:271-302 | **18+ 操作**:prompt(文本/图像/消息)/skill/promptFromTemplate/compact(navigateTree/resume/abort/**steer/followUp/nextRun 三队列**/cancelQueued/recordUsage/waitForIdle/runWhenIdle/peekAction/executeAction/runToCompletion/getModel/setModel/getThinkingLevel/setThinkingLevel/getActiveTools/setActiveTools/watch |
| **错误分类学** | :28-81 | **14 个 TaggedError**:LaneBusy/MissingIdentities/NoActiveRun/NoActiveOperation/NothingToResume/InvalidMessage/UnknownSkill/UnknownTemplate/UnknownTarget/UnknownQueueItem/LaneExists/InvalidLane/NothingToCompact/Closed——**每个错误带结构化字段**(lane/reason/targetId) |
| **默认值** | :328-341 | compaction{enabled,reserveTokens:16384,keepRecentTokens:20000};steeringMode/followUpMode="one-at-a-time";retryPolicy 默认禁用 |
| **hooks/events 注册** | :343-345 | UnavailableRegistry(hooks.on/events.on,closed 后不可用)——**关闭后注册拒绝** |
| **挂起恢复** | create() | findRecords 有记录 → HarnessNotImplemented(create.restore)——**恢复路径未实现时明确失败** |

---

## 二、关键设计(通用价值)

1. **"三队列 + 双向操作"**:steer/followUp/nextRun 三队列 + prompt/abort/resume 全集——**会话驱动的完整命令面**(与 Reasonix Controller 命令面同族)
2. **"错误分类学"**:14 个结构化错误——**失败可编程处理**(调用方可按类型分支,与 SessionErrorCode 同思想)
3. **"关闭后注册拒绝"**:UnavailableRegistry——**生命周期关闭的正确性**

**产品②执行引擎映射**:AgentLane 契约 = 执行引擎的命令面全集(18+ 操作),错误分类学 = 失败模式的编程化。

---

## 三、累计覆盖对账

| 轮次 | 层面 | 新增 | 累计 |
|:--:|------|:--:|:--:|
| v1-v29 | — | 127 | 127 |
| v30 | agent-harness 主体 | +0(深化 2 设计) | **127**(深化) |

> 剩余:harness tools 两套实现对比(bash/read/edit harness vs core)——下一轮。
