# Pi 域发现 v28 补充(续扫第十九轮:provider-composer 内部)— 2026-08-14

> 承接:v27。本轮:provider-composer.ts(572)内部——多模型编排的 Pi 实现。
> 结论:composeModelProvider 确认(配置+认证+惰性流的组装工厂),无新域,深化 provider 域。本轮后核心实现层基本全部覆盖。

---

## 一、v28 深化确认(provider-composer 内部)

| 设计 | 位置 | 要点 |
|------|------|------|
| **composeModelProvider** | provider-composer.ts:420-524 | 配置(models.json)+ 认证(api-key/oauth/command)+ **lazyStream 惰性流**的组装工厂;兼容请求配置 resolveCompatibilityRequestConfig |
| **ExtensionOAuthConfig** | :33-44 | 扩展 OAuth 桥:login/refreshToken/getApiKey/**modifyModels**(登录后可改模型列表) |
| **ProviderConfigInput** | :46+ | 扩展 registerProvider API 的配置输入(provider/api/baseUrl/apiKey) |
| **configContextEnv** | :287+ | 配置上下文 env 解析 |
| **认证解析** | :341-405 | toAuth(credential→apiKey)/check/resolve |

---

## 二、关键设计(通用价值)

1. **"组装工厂"**:配置+认证+流 的组合是 provider 层的统一装配点——**provider 层的一致性架构**
2. **"登录后可改模型"**:modifyModels(OAuth 登录后扩展可改模型列表)——**动态模型发现**
3. **"惰性流"**:lazyStream 流延迟创建——**资源惰性化**

---

## 三、累计覆盖对账(核心实现层收官)

| 轮次 | 层面 | 新增 | 累计 |
|:--:|------|:--:|:--:|
| v1-v27 | — | 127 | 127 |
| v28 | provider-composer 内部 | +0(深化) | **127**(深化) |

**Pi 深挖 19 轮完成(127 域 + 30+ 契约深化)**:
- 覆盖:全部源码文件(341)+ 全部测试文件(474,除 tui 视觉)+ 核心实现层(settings/resource-loader/model/composer/compaction/sdk 内部)
- 剩余:agent-session 状态机内部(已 q1)/工具实现(已 q5)/生成物/视觉——全部已有笔记覆盖或排除清单
- **"感觉到底"在 Pi 上已 7 次被证伪**,但最终的程序化覆盖核对(文件 × 测试 × 实现层三清单)已全部通过
