# Pi 域发现 v8 补充(第二轮深扫:存储/工具/评测层)— 2026-08-14

> 承接:v7 补充(体量排序复测)。v8 按 Hermes 逐层法继续:session-backends/storage、coding-agent/tools、ai/auth、evals/telemetry。
> 结论:**再发现 3 个 load-bearing 级遗漏(Fencing Token 实现/seq 全序/文件变异队列)+ 7 个中价值**。

---

## 一、v8 新增域

### 🔴 高价值新增(3 个)

| # | 域 | 文件 | 体量 | 设计要点 | 产品映射 |
|---|----|------|:--:|---------|---------|
| 76 | **Fencing Token 实现** | session-backends/sqlite-node/src/sqlite/storage/writer-leases.ts | 58 | **分布式栅栏的 SQLite 实现**:owner_id + fence 单调递增 + expires_at;续租条件 `WHERE owner_id=? AND fence=?`(防 stale 续租);acquire/renew 原子。q7 只提理论,这是实现 | ④知识库(多进程写栅栏) |
| 77 | **seq 全序实现** | storage/session-sequences.ts | 29 | createSequence/getNext/advance(单调 seq 的事件溯源全序)——q7 提理论,这是实现 | ④知识库(重放全序) |
| 78 | **文件变异队列** | core/tools/file-mutation-queue.ts + harness/tools/file-mutation-queue.ts | ~40 | **同文件变异串行化**(realpath 规范化 key + promise 链),不同文件仍并行——与 Hermes file_state 同构 | ②执行(并发写安全) |

### 🟡 中价值新增(7 个)

| # | 域 | 文件 | 设计要点 |
|---|----|------|---------|
| 79 | **工具定义包装** | core/tools/tool-definition-wrapper.ts | ToolDefinition → AgentTool 包装(extensions 与 core 工具的桥) |
| 80 | **图片 MIME 检测** | harness/tools/image.ts | 魔数检测(JPEG/PNG/动画 PNG 排除/GIF/WebP/BMP)+ base64 编码 |
| 81 | **OAuth 流族(补齐)** | ai/src/auth/oauth/(anthropic 364/copilot 417/device-code/pkce/openai-codex 544/radius 403/kimi 310/xai 239/openrouter 311) | 各 provider OAuth 流(设备码/PKCE/页面对话) |
| 82 | **eval harness** | evals/src/pi-harness.ts(257)+ vitest-evals/(summary 438/harness-table 193/artifacts 113/reporter 111) | **真实 agent 评测**:createAgentSessionServices + Harness + transcript 事件 + 会话快照 artifact |
| 83 | **遥测包** | telemetry/src/(index 357/memory 219/testing conformance 315) | 遥测 API + 内存遥测 + conformance |
| 84 | **分支缓存** | sqlite/branch-cache.ts(101) | branch 状态缓存 |
| 85 | **writer 租约表** | storage/(sessions 131/lanes 124/records 95/entries 78/facts 64/session-stats 54/branch-tips 35) | 各存储表实现(域发现已覆盖表模型,补实现文件) |

---

## 二、累计覆盖对账(Pi 全项目)

| 包 | 源码文件数 | v1-v6 覆盖 | v7+v8 复测 | 状态 |
|----|:--:|------|------|------|
| ai/ | ~180 | 主要架构 | +api 适配族 + OAuth 族 | 已补 |
| coding-agent/ | ~190 | 核心 | +interactive-mode + runner + event-bus + file-mutation-queue + http-dispatcher | 已补 |
| agent/ | 49 | 核心 | +reducer + events | 已补 |
| session-backends/ | 22 | 表模型 | +writer-leases(Fencing)+ seq 全序 + branch-cache | 已补 |
| tui/ | ~40 | 排除 | 保留 keybindings | 已补 |
| server/client/protocol | ~30 | 保留 | 已核对 | 完成 |
| evals/telemetry | ~15 | 一句话 | +pi-harness + telemetry 包 | 已补 |

**遗漏归零确认**:top 150 文件逐一核对 + 全部子目录扫描完成,无剩余未核对文件。

---

## 三、总教训(Hermes 反哺 Pi 的完整闭环)

1. **体量排序是穷尽性第一工具**:Pi 最大文件 interactive-mode(6,436)漏了 6 轮;Hermes 靠它 0 遗漏
2. **理论层发现≠实现层发现**:q7 提了 Fencing Token/seq 全序理论,但没记录 writer-leases.ts/session-sequences.ts 的实现位置——**域发现要记录理论+实现双位置**
3. **跨项目同构印证**:file-mutation-queue(Pi)↔ file_state(Hermes)、delivery_ledger(Hermes)↔ writer-leases+reducer(Pi)——机制重复出现=产品必须抄的通用模式
4. **#24 完整实证链**:Pi(假收敛 59)→ Hermes(被逼出 80)→ Pi 复测(+16 域)→ 方法论修正(体量排序强制)

---

## 四、处置(并入 v7 的 HANDOVER 待办)

- [ ] Pi 闭环笔记补:reducer 损坏检测 + writer-leases(Fencing 实现)+ seq 全序 + file-mutation-queue
- [ ] Pi 参考架构检查并补:reducer 损坏检测 + Fencing 实现
- [ ] 方法论 D13 修正:域发现 Pass 1 增加"体量排序 top N 核对"+"理论层实现位置记录"
- [ ] 两补充文件(v7/v8)合并进 Pi 域发现主文档
