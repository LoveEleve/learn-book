# Pi 域发现(深度 Review 版)— 逐文件核对

> 项目:earendil-works/pi(main,5,663 commits)
> 版本:v2 — 2026-08-14,530 个源码文件全部核对
> v1 问题:遗漏了 skills、search、session-manager、settings-manager、resource-loader、provider-composer 等关键设计

---

## 一、总览:530 文件分布

| 包 | 源码文件数 | 体量 | 定位 |
|----|:--:|------|------|
| ai | ~180 | 大 | LLM 统一抽象(provider/api/auth/models 四层) |
| coding-agent | ~190 | 最大 | 编码 agent 完整实现 |
| agent | 49 | 中 | 纯 agent 运行时(harness + loop) |
| tui | ~40 | 中 | 终端 UI(排除视觉,保留原生层) |
| session-backends | 22 | 小 | SQLite 持久化 |
| server/client/protocol | ~30 | 小 | headless server + 通信 |
| telemetry/evals | ~15 | 小 | 遥测/评测 |

---

## 二、域清单(穷尽版,按包逐文件核对)

### 🔴 核心域(12 个)

| # | 域 | 位置(关键文件) | 设计决策 | 产品映射 |
|---|----|--------------|---------|---------|
| 1 | **Agent Loop** | agent/src/agent-loop.ts | 循环主体 + `agentLoopContinue` 续跑 | ②执行引擎 |
| 2 | **AgentSession 状态机** | coding-agent/src/core/agent-session.ts(3344 行) | 会话级状态机:steering/follow-up/pending/compaction/retry 全生命周期 | ②执行契约 |
| 3 | **SessionManager** | coding-agent/src/core/session-manager.ts(1714 行) | 会话创建/恢复/管理 | ④知识库 |
| 4 | **Harness 上下文管理** | agent/src/harness/agent-harness.ts + session/ | 上下文组装/注入/会话状态 | ①对齐+② |
| 5 | **上下文压缩 Compaction** | harness/compaction/ + core/compaction/ | branch-summarization(分支摘要) | ②执行契约 |
| 6 | **工具系统(两套)** | agent/src/harness/tools/(bash/read/write/edit/image) + coding-agent/src/core/tools/(+grep/find/ls) | 工具定义/执行/输出处理;**发现:harness 层和 core 层各有工具集** | ②执行 |
| 7 | **会话持久化 SQLite** | session-backends/sqlite-node/ | repo(953 行)+ branch/entries/facts/lanes/records 存储模型 | ④知识库 |
| 8 | **LLM 统一抽象** | ai/src/(provider/api/auth/models) | 多供应商 provider + API 适配层 + OAuth + 模型目录 | ②底层 |
| 9 | **Skills 系统** | agent/src/harness/skills.ts(375 行)+ coding-agent/src/core/skills.ts(487 行) | **v1 遗漏!** 技能定义/加载/调用 | ③+④ |
| 10 | **Settings 管理** | coding-agent/src/core/settings-manager.ts(1290 行) | **v1 遗漏!** 设置持久化/更新/默认值 | ①对齐 |
| 11 | **ResourceLoader** | coding-agent/src/core/resource-loader.ts(1096 行) | **v1 遗漏!** 资源加载/上下文注入 | ①对齐 |
| 12 | **Provider Composer** | coding-agent/src/core/provider-composer.ts(572 行) | **v1 遗漏!** 多模型编排/组合 | ②执行 |

### 🟡 支撑域(13 个)

| # | 域 | 位置 | 设计决策 |
|---|----|------|---------|
| 13 | Model 管理 | core/model-registry/model-resolver/model-runtime/model-config | 模型解析/选择/运行时 |
| 14 | Modes | coding-agent/src/modes/(interactive/rpc/print) | 交互/非交互执行模式 |
| 15 | 扩展系统 Extensions | core/extensions/ + extensions/llama | agent 自扩展(self-extensible) |
| 16 | 权限/信任 | core/project-trust.ts + trust-manager.ts + cli/project-trust.ts | 目录信任机制 |
| 17 | Server/Client | server/ + client/ + protocol/ | headless 运行 + 进程间通信 |
| 18 | SDK | core/sdk.ts(401 行) | **v1 遗漏!** 对外编程接口 |
| 19 | Slash Commands | core/slash-commands.ts | **v1 遗漏!** 斜杠命令系统 |
| 20 | Output Guard | core/output-guard.ts | 输出防护 |
| 21 | Search | agent/src/search/(scanning.ts) | **v1 遗漏!** 会话搜索 |
| 22 | Package Manager | core/package-manager.ts(2677 行) | 依赖管理工具执行 |
| 23 | Telemetry | telemetry/ + core/telemetry.ts | 遥测 |
| 24 | Auth Storage | core/auth-storage.ts(507 行) | 认证信息持久化 |
| 25 | Config | config.ts + core/resolve-config-value.ts | 配置解析 |

### 🟢 v3 新增(第 3 轮 review 补充)

| # | 域 | 位置 | 设计决策 | 产品映射 |
|---|----|------|---------|---------|
| 26 | **CLI 会话管理** | cli/session-picker.ts + client/remote-session.ts + client/transcript.ts | 会话选择/远程会话/对话记录 | ④知识库 |
| 27 | **CLI 启动流程** | cli/initial-message.ts + cli/file-processor.ts + cli/startup-ui.ts | 初始消息注入/文件参数处理 | ①对齐 |
| 28 | **Experimental CLI** | cli/experimental/ | 新 CLI 命令实验区 | — |
| 29 | **Server 会话快照** | server/snapshots.ts + server/sessions.ts | 服务端会话/快照 | ④知识库 |
| 30 | **Print Mode** | modes/print-mode.ts + json-event.ts | 非交互输出模式 | ②执行 |
| 31 | **RPC Mode** | modes/rpc/(rpc-mode/rpc-client/rpc-types) | RPC 交互协议 | — |
| 32 | **Llama 扩展示例** | extensions/llama/(client/provider/ui) | 扩展系统真实示例 | ③扩展 |
| 33 | **Runtime Credentials** | core/runtime-credentials.ts | **v3 遗漏补!** 运行时凭证 | 安全 |
| 34 | **Session CWD** | core/session-cwd.ts | 会话工作目录管理 | ②执行 |
| 35 | **Usage Totals** | core/usage-totals.ts | token 用量统计 | 成本 |

### 🟢 v4 新增(第 4 轮 review 补充)

| # | 域 | 位置 | 设计决策 | 产品映射 |
|---|----|------|---------|---------|
| 36 | **Images 体系** | ai/src/images*.ts(116 行) | 图像生成 API 抽象(注册表/模型) | — |
| 37 | **Tools Manager** | coding-agent/src/utils/tools-manager.ts(374 行) | 工具生命周期管理 | ②执行 |
| 38 | **FS Watch** | utils/fs-watch.ts + tui/undo-stack + kill-ring | 文件监控/编辑撤销栈 | ②执行 |
| 39 | **Git 封装** | utils/git.ts(226 行) | git 操作封装 | ②执行 |
| 40 | **Client 会话句柄** | client/src/session-handle.ts + state.ts | 客户端会话状态/句柄 | ④知识库 |
| 41 | **Unix Transport** | server/src/transports/unix/ + client/src/unix.ts | Unix socket 通信(系统编程面) | — |
| 42 | **Protocol 编解码** | protocol/src/(codec/framing/schemas/cbor) | CBOR 协议编解码 | — |
| 43 | **终端输入体系** | tui/src/(stdin-buffer 444/keys 1401/terminal 559) | 终端输入/按键/原生层(系统编程面) | — |
| 44 | **ai 兼容层** | ai/src/compat.ts(298) + legacy-api-aliases.ts | API 兼容/别名 | — |
| 45 | **Clippy 工具集** | coding-agent/src/utils/(child-process/shell/mime/paths/html/image-*) | 编码 agent 通用工具库 | — |

### 🟢 v5 新增(第 5 轮 review 补充 — 高价值)

| # | 域 | 位置 | 设计决策 | 产品映射 |
|---|----|------|---------|---------|
| 46 | **提示词模板系统** | harness/prompt-templates.ts(262 行) | 模板参数替换/命令解析——**对齐模块直接可抄** | ①对齐 |
| 47 | **Compaction 提示词协议** | harness/messages.ts(168 行) | COMPACTION_SUMMARY_PREFIX/SUFFIX 约定——**压缩消息的提示词协议** | ②执行 |
| 48 | **结构化约束采样** | ai/src/api/constrained-sampling.ts(277 行) | JSON Schema strict 模式/grammar 采样——**让 agent 输出结构化规格书的关键** | ①对齐+② |
| 49 | **全局事实存储(Session Facts)** | session-backends facts 表 + session/types.ts Session 接口 | `(session_id, seq, kind, key, value)` 模型 + setName/setLabel/getLabel——**产品④书级知识库的直接蓝图** | ④知识库 |
| 50 | **事件溯源模型(Records/Lanes)** | storage/records.ts + lanes.ts | `(lane, run_id, op_kind, timestamp, payload)` + 恢复语义(0 空闲/1 挂起/2 损坏) | ④知识库 |
| 51 | **执行环境抽象** | harness/env/nodejs.ts(695 行) NodeExecutionEnv | 执行环境接口 | ②执行 |
| 52 | **遥测 Schema** | harness/telemetry.ts(615 行) | AI_TELEMETRY_SCHEMA 结构化遥测 | ③验收 |
| 53 | **OAuth 设备码/PKCE** | ai/src/auth/oauth/(device-code/pkce) | 设备码流认证 | — |
| 54 | **参数约束(上下文裁剪)** | ai/src/api/simple-options.ts(clampMaxTokensToContext) | maxTokens 按上下文裁剪 | ②执行 |

### 🟢 v6 新增(第 6 轮 review — JD 关键词覆盖测试补)

**触发**:用 JD 关键词 × 域清单做完备性测试,当场抓到 3 个遗漏。

| # | 域 | 位置 | 设计决策 | 状态 |
|---|----|------|---------|------|
| 55 | **会话状态变更协议** | harness/session/state.ts + jsonl/codec.ts | SessionMutation 4 种(entry/record/lane/fact)+ JSONL v4 编解码 | ✅ 高价值 |
| 56 | **上下文构建管线** | harness/session/context.ts | entries→messages 转换、compaction 后只保留摘要、entryProjectors 投影 | ✅ 高价值 |
| 57 | **Sandbox(最小实现)** | bun/restore-sandbox-env.ts(36 行)+ bun/cli.ts | 沙箱环境恢复——**实现极小,但这是 Pi 的沙箱设计** | ✅ 补入 |
| 58 | **MCP(设计决策:不支持)** | 全仓库无 mcp 引用(仅 vendored JS 碰巧含词) | **Pi 架构决策:不集成 MCP**——对比 Reasonix/OpenCode 有 MCP,这是差异点 | ⚠️ 标注为"无此域"决策 |
| 59 | **细粒度权限(设计决策:不支持)** | core 无 permission 文件,仅 project-trust | **Pi 架构决策:只有目录级信任,无工具级权限**——对比 OpenCode 的 permission 系统 | ⚠️ 标注为"无此域"决策 |

### 🟢 v7 新增(第 7 轮 review — 文件体量排序复测)

**触发**:Hermes 深度 review 教训反哺(Hermes 27→80 域)。按"文件体量排序 top N 逐一核对"取代"按目录感觉扫描",561 个源码文件排序后对 top 150 逐一核对。

| # | 域 | 位置 | 体量 | 设计决策 | 产品映射 |
|---|----|------|:--:|---------|---------|
| 60 | **事件归约器(RecordLog)** | agent/src/harness/reducer.ts | 667 | **单写者记录协议正确性层**:RecordLogCorruption 12 种损坏原因(multiple_open_operations/non_consecutive_attempt/tool_call_mismatch/duplicate_tool_invocation…);restore **拒绝**损坏态而非修复 | ④重放损坏检测 |
| 61 | **交互形态(InteractiveMode)** | coding-agent/src/modes/interactive/interactive-mode.ts | **6,436(项目最大)** | TUI 渲染 + 会话业务代理;流式消息/工具执行跟踪/thinking 可见性/skill 命令/编辑器 bash 模式;agent 订阅解耦 | ①交互形态 |
| 62 | **扩展运行时(ExtensionRunner)** | core/extensions/runner.ts | 1,236 | 完整事件钩子契约(BeforeAgentStart/BeforeProviderRequest/ContextEvent/CompactOptions/InputEvent)+ 生命周期 | ③钩子契约 |
| 63 | **事件总线** | core/event-bus.ts + harness/events.ts | 60+102 | emit/on/clear + **handler 错误隔离**(async safeHandler) | ②事件分发 |
| 64 | **会话运行时创建** | core/agent-session-runtime.ts(441)+ services.ts(221) | 662 | session 创建 + cwd 绑定服务 + 运行时诊断(非致命问题收集,app 层决定) | ②执行 |
| 65 | **资源冲突诊断** | core/diagnostics.ts | — | **ResourceCollision**:extension/skill/prompt/theme 资源名冲突,winner/loser 路径 + 来源 | ④引用完整性 |
| 66 | **provider 归因** | core/provider-attribution.ts | — | 模型→provider 识别(主机匹配) | — |
| 67 | **API 适配族(补齐)** | ai/src/api/(codex-responses 1,647/bedrock 1,188/mistral 931/vertex 596/pi-messages 433/transform-messages 223) | 5,911 | 各 provider API 适配;transform-messages 含**非视觉模型图片降级占位符** | ②provider 层 |
| 68 | **HTTP 分发器** | core/http-dispatcher.ts | — | 全局 fetch 安装/HTTP 空闲超时(30s-5min/禁用) | — |
| 69 | **输出累加器** | core/tools/output-accumulator.ts(222)+ truncate.ts(276) | 498 | **流式输出有界内存**(maxLines/maxBytes/临时文件溢出) | ②执行 |
| 70 | **键位系统** | core/keybindings.ts(370)+ tui/keybindings.ts(320) | 690 | app.* 键位定义(interrupt/clear/exit/suspend) | — |
| 71 | **SQLite 搜索后端** | session-backends/.../search-backend.ts | 194 | SessionSearch 接口实现(FTS/中止信号) | ④检索 |
| 72 | **模型商店** | core/models-store.ts + model-config.ts | 444 | 模型配置/商店访问 | — |
| 73 | **导出 HTML** | core/export-html/(316+258+172) | 746 | 会话导出 HTML(ANSI 转换) | ③产出物 |
| 74 | **配置迁移** | coding-agent/src/migrations.ts | 315 | 配置版本迁移 | — |
| 75 | **消息变换** | ai/src/api/transform-messages.ts | 223 | 非视觉模型图片降级(placeholder 防重复) | ②provider 层 |

### 🟢 v8 新增(第 8 轮 review — 子目录深扫)

**触发**:继续 Hermes 逐层法,扫描 session-backends/storage、core/tools、ai/auth、evals/telemetry。

| # | 域 | 位置 | 体量 | 设计决策 | 产品映射 |
|---|----|------|:--:|---------|---------|
| 76 | **Fencing Token 实现** | sqlite/storage/writer-leases.ts | 58 | **分布式栅栏的 SQLite 实现**:owner_id + fence 单调递增 + expires_at;续租 `WHERE owner_id=? AND fence=?`(防 stale);acquire/renew 原子——q7 只提理论,这是实现 | ④多进程写栅栏 |
| 77 | **seq 全序实现** | sqlite/storage/session-sequences.ts | 29 | createSequence/getNext/advance 单调 seq——事件溯源全序的实现 | ④重放全序 |
| 78 | **文件变异队列** | core/tools/file-mutation-queue.ts + harness 版 | ~40 | **同文件变异串行化**(realpath key + promise 链),不同文件并行——与 Hermes file_state 同构 | ②并发写安全 |
| 79 | **工具定义包装** | core/tools/tool-definition-wrapper.ts | — | ToolDefinition → AgentTool 桥(extensions ↔ core) | ③扩展 |
| 80 | **图片 MIME 检测** | harness/tools/image.ts | — | 魔数检测(JPEG/PNG/动画排除/GIF/WebP/BMP) | — |
| 81 | **OAuth 流族(补齐)** | ai/src/auth/oauth/(anthropic 364/copilot 417/codex 544/radius 403/kimi 310/xai 239) | 2,277 | 各 provider OAuth(设备码/PKCE/页面) | 凭证 |
| 82 | **eval harness** | evals/src/pi-harness.ts(257)+ vitest-evals/ | 1,100 | **真实 agent 评测**:createAgentSessionServices + Harness + transcript 事件 + 会话快照 artifact | ③评测 |
| 83 | **遥测包** | telemetry/src/(index 357/memory 219/conformance 315) | 891 | 遥测 API + 内存遥测 + conformance | 可观测 |
| 84 | **分支缓存** | sqlite/branch-cache.ts | 101 | branch 状态缓存 | ④ |
| 85 | **存储表实现(补齐)** | storage/(sessions 131/lanes 124/records 95/entries 78/facts 64/stats 54/tips 35) | 581 | 各存储表实现(域发现已覆盖表模型,补实现文件) | ④ |

### 🟢 v9 新增(第 9 轮 review — 测试契约层 + 模式共享内核)

**触发**:打开 tests 契约层(474 测试文件/115K 行)验证"测试即行为契约";回答遗留 Q9/Q10。

| # | 域 | 位置 | 设计要点 | 产品映射 |
|---|----|------|---------|---------|
| 86 | **并发回合契约** | coding-agent/test/agent-session-concurrent.test.ts | prompt() 流式时抛错但 steer/followUp 允许;**扩展起源 steer 排队**;agent 事件先于 tool_call 发出;慢扩展处理器下 message_end 有序持久化 | ②并发安全契约 |
| 87 | **序列化契约** | test/compaction-serialization.test.ts | serializeConversation:长工具结果截断/短不截断/assistant+user 永不截断 | ②压缩契约 |
| 88 | **归约器契约** | agent/test/harness/reducer.test.ts(1,127 行) | lane 状态归约 20+ 不变量:abort 杀 steer+followUp 队列保写入/孤儿 result 忽略/工具批不从未决 deferred-write 解析/溢出守卫仅在新对话输入后重置/确定性 | ④归约正确性 |

**v9 关键发现(回答 Q9/Q10)**:
- **Q9 已证**:interactive/print/rpc 三模式共享 `AgentSessionRuntime` 内核(interactive-mode.ts:392 / print-mode.ts:33 / rpc-mode.ts:54 都注入 runtimeHost;AgentSessionRuntime = session + services + rebind 回调 + 扩展事件桥 session_before_switch/fork 可取消钩子)。**产品②"多形态只换外壳"是 agent 通用模式**(与 Hermes 同一 AIAgent 跑四形态同构)
- **Q10 已答**:session-selector.ts(1,031)+ search(194)是完整会话选择/搜索/重命名/删除交互,产品④"跨 session 延续"UI 参考(低优先级)

### 🟢 扫描域(简扫)

| 域 | 位置 | 说明 |
|----|------|------|
| TUI 原生层 | tui/src/(native-modifiers/stdin-buffer/keys/terminal) | 终端系统编程,保留 |
| evals | evals/src/ | 评测基础设施 |
| Bash Executor | core/bash-executor.ts | 命令执行封装 |
| CLI | coding-agent/src/cli/ | 命令入口 |

---

## 三、排除清单(核对后)

| 排除 | 原因 |
|------|------|
| tui/src/components/ 全部视觉组件 | 前端 UI 视觉层 |
| ai/src/providers/*.models.ts(~50 个) | 生成的模型元数据 |
| ai/src/api/*.lazy.ts | 懒加载包装 |
| ai/src/providers/ 各供应商实现(保留 deepseek/openai/anthropic 作样本即可) | 同构重复 |
| agent/src/harness/env/ | vendor 环境 |
| **注意:ai 的 auth/(OAuth 设备码/PKCE)保留** — 凭证管理是通用设计 |

---

## 四、关键发现(深度核对新增)

### 发现 1:两套工具系统
- `agent/src/harness/tools/`(通用:bash/read/write/edit/image)
- `coding-agent/src/core/tools/`(编码专用:+grep/find/ls/file-mutation-queue)
- **问题**:两套关系?core 复用 harness 还是独立?→ 需深挖

### 发现 2:AgentSession 是核心中的核心(3344 行)
状态机包含:steering(引导消息)、follow-up(追问)、pending(待发)、compaction(压缩)、overflow recovery、retry、bash abort 控制、extension runner、model runtime。
→ **这是"对齐 → 执行"状态管理的完整样本,产品 ② 必深挖**

### 发现 3:branch-summarization 在两层都有
- harness/compaction/branch-summarization.ts
- core/compaction/branch-summarization.ts
→ 两套实现?harness 版是抽象,core 版是编码 agent 具体版?

### 发现 4:provider-composer(572 行)
多模型组合能力——对应 Reasonix 的 executor+planner 双模型设计
→ 产品多模型编排参考

### 发现 5:skills 在 harness 和 core 都有
harness 版 375 行 + core 版 487 行 → 技能系统有两层抽象

### 发现 6(v3):CLI 会话层设计
- cli/session-picker(会话选择) + client/remote-session(远程会话) + transcript(对话记录)
- → 多会话管理/远程连接的完整设计,产品④参考

### 发现 7(v3):Modes 三模式
- interactive(完整交互)/ print(非交互单次输出)/ rpc(协议驱动)
- → 同一 agent 内核,三种执行形态 = 对齐后"自主跑"的形态参考

### 发现 8(v3):extensions/llama 是完整扩展示例
llama(client/provider/ui)展示了扩展系统如何接入新 provider + 自定义 UI
→ 产品③扩展能力参考

### 发现 9(v4):终端输入体系是隐藏的复杂域
stdin-buffer(444 行)+ keys(1401 行)+ terminal(559 行)——按键解析/终端控制是系统编程面
→ 注意:tui/keys 1401 行说明终端交互工程量大,但产品(源码学习 agent)不需要

### 发现 10(v4):Protocol 用 CBOR
protocol/src/cbor/(encoder 216/decoder 168)——进程间通信用 CBOR 而非 JSON
→ server/client 架构的序列化选择

### 发现 11(v4):client 是完整 SDK 面
client/src/(client 432/connection 236/unix 156/state 156)——跨进程调用的客户端状态管理
→ 产品④如果做"服务端知识库 + 客户端交互"可参考

### 发现 12(v5):Session Facts = 书级知识库的直接蓝图
`Session` 接口(agent/src/harness/session/types.ts:315-335)暴露:
- `setName/getName` — 会话命名(全局事实)
- `setLabel/getLabel(id, label)` — **给任意条目打标签**("结论/证据/坑"语义可直接映射)
- `findOpenOperations(lane, limit)` — 恢复语义:0=空闲/1=挂起/2=损坏
- `getLog(afterSeq)` — 增量日志
- 底层 facts 表:`(session_id, seq, kind, key, value)`,kind='label' 已实际使用

→ **产品④(书级知识库)可以直接复刻这个模型**:结论=facts(kind=conclusion)、证据=label 引用、章节=lane、恢复=findOpenOperations。

### 发现 13(v5):结构化约束采样 = 对齐模块的关键技术
`ai/src/api/constrained-sampling.ts`(277 行):makeStrictJsonSchema / GrammarConstrainedSampling
→ **让 agent 输出严格 JSON 规格书(执行契约)的机制**,对齐模块的 A 档输出可以强制走 strict schema。

### 发现 14(v5):恢复语义已经内建
findOpenOperations 的"0 空闲/1 挂起/2 损坏"三态判定——**会话中断恢复的设计**,产品④的"从知识库恢复进度"直接可用。

---

## 五、Pass 2 深挖顺序(修正版)

按依赖顺序 + 产品优先级:

1. **AgentSession 状态机**(core/agent-session.ts,3344 行)— 产品②核心
2. **SessionManager**(1714 行)— 产品④
3. **Harness 上下文**(agent-harness.ts + harness/session/)— 产品①②
4. **Compaction**(两层)— 产品②
5. **SQLite 存储模型**(repo + storage/)— 产品④
6. **Skills**(两层)— 产品③④
7. **Provider Composer** — 多模型
8. **Settings/ResourceLoader** — 产品①
9. **工具系统**(两套)— 产品②
10. **Agent Loop** — 产品②
11. **Output Guard / Slash Commands / SDK** — 按需
12. **ai/ 认证层** — 凭证管理

---

## 六、待验证问题(深挖时回答)

- [ ] Q1:两套工具系统(harnelss vs core)的关系?
- [ ] Q2:两套 compaction 的关系?
- [ ] Q3:AgentSession 如何驱动 agent-loop?(调用方向)
- [ ] Q4:SQLite 的 branch/lane/fact 模型能否承载"结论+证据"式知识库?
- [ ] Q5:Skills 的加载/调用协议?(对齐模块要用)
- [ ] Q6:provider-composer 的编排方式?(多模型参考)
- [ ] Q7:SettingsManager 的默认值/持久化模式?(对齐模块参考)
- [ ] Q8:ResourceLoader 加载什么资源?如何注入上下文?(对齐模块参考)
- [ ] Q9(v3):print-mode 与 interactive-mode 共享什么内核?(自主执行形态参考)
- [ ] Q10(v3):session-picker 如何列出/选择历史会话?(产品④)

---

## 七、Review 收敛记录(元观察)

| 轮次 | 触发 | 新增域 | 累计 | 含金量 |
|:--:|------|:--:|:--:|------|
| v1 | 初始粗扫 | 13 | 13 | 核心 |
| v2 | 深度 review | +12 | 25 | 核心(skills/session-manager) |
| v3 | 再 review | +10 | 35 | 中(cli/modes/llama) |
| v4 | 三 review | +10 | 45 | 小(images/fs-watch/transport) |
| v5 | 四 review | +9 | 54 | **高(重新聚焦!)**:facts 存储/约束采样/提示词模板/恢复语义 |
| v6 | JD 关键词覆盖 | +5 | 59 | 高(SessionMutation/上下文管线/沙箱) |
| v7 | 体量排序复测(Hermes 反哺) | +16 | 75 | **高(假收敛证伪)**:reducer/interactive-mode/runner/event-bus |
| v8 | 子目录深扫 | +10 | 85 | 高(Fencing 实现/seq 全序/file-mutation-queue) |
| v9 | 测试契约层 | +3 | 88 | 高(并发契约/归约器契约);Q9/Q10 已答(三模式共享内核) |

**关键观察(v5 打破预期)**:
- 原以为"增量衰减、即将收敛",但 v5 打开 harness/session 接口后**重新发现高价值域**(facts=书级知识库蓝图、constrained-sampling=结构化输出)
- **规律修正**:review 的"含金量"不是单调递减——**取决于这轮 review 打开了哪个层面**。浅层文件(utils/images)→ 低价值;深层接口(harness/session/types)→ 高价值
- **对 #24 的修正**:收敛性检查不能只按"新增数量"判定,要按"是否到达深层结构"判定——**穷尽性的正确指标是"所有接口/类型已读完",不是"新增数为零"**

**v7/v8 追加观察(假收敛证伪,2026-08-14)**:
- **v1-v6 的 59 域是假收敛**:按体量排序复测,发现 interactive-mode.ts(6,436 行,项目最大文件)6 轮 review 都没进视野;reducer.ts(事件溯源损坏检测核心)完全遗漏
- **理论层发现≠实现层发现**:q7 提了 Fencing Token/seq 全序理论,但 v8 才找到 writer-leases.ts/session-sequences.ts 的实现——**域发现必须记录理论+实现双位置**
- **方法论修正(Hermes 实证)**:域发现 Pass 1 必须含"文件体量排序 top N 逐一核对",禁止按目录感觉扫描;体量排序是穷尽性检查的第一工具
- **跨项目同构印证**:file-mutation-queue(Pi)↔ file_state(Hermes)、writer-leases+reducer(Pi)↔ delivery_ledger(Hermes)——机制重复出现 = 产品必须抄的通用模式
- **剩余**:top 150 文件 + 全部子目录已核对,遗漏归零。未深挖项(harness/session 具体实现、agent-loop 本体、compaction 两层)是 Pass 2 闭环笔记的活

### 🟢 v10/v11 新增(续扫第 10-11 轮 — auth/工具加载/搜索/server/存储/遥测层)

**触发**:Reasonix 深挖到 101 域后用户指出"Pi 是否也没探索完毕?"——用同规格逐目录核对继续 Pi。**确认 Pi 也未探索完毕**(88 域后仍有遗漏),两轮 +20 域至 108。

| # | 域 | 位置 | 设计要点 | 产品映射 |
|---|----|------|---------|---------|
| 89 | 凭证存储 | ai/src/auth/(credential-store/context/resolve) | **InMemoryCredentialStore**:每 provider 一凭证,写按 provider 串行化(promise 链);AuthContext 抽象(env/文件存在,浏览器安全) | 凭证 |
| 90 | 工具延迟加载 | ai/src/utils/deferred-tools.ts | **从 transcript 按使用拆分 immediate/deferred 工具**——与 Hermes tool_search 同构 | ②上下文节省 |
| 91 | 扫描式会话搜索 | agent/src/search/scanning.ts | 流式全扫描:投影器/分页/中止;SessionSearchHit(sessionId/seq/snippet)——Pi 搜索形态(非 FTS) | ④检索 |
| 92 | CLI 子命令族 | coding-agent/src/cli/(args/auth-check/auth-command/list-models) | CLI 启动/认证/模型列表 | — |
| 93 | 远程模型目录 | core/remote-catalog-provider.ts | 远程目录 4h 刷新 + mergeModels(动态覆盖基线) | — |
| 94 | 代理流函数 | agent/src/proxy.ts | **经 server 路由 LLM 调用**(客户端模式) | ② |
| 95 | 默认流函数 | agent/src/stream-fn.ts | setDefaultStreamFn 注入(host 提供默认 runtime) | — |
| 96 | 管理 HTTP | utils/management-http.ts | fetchWithRetry(408/425/429/500/502/503/504);仅限幂等管理请求 | — |
| 97 | 服务端快照 | server/snapshots.ts | revision 递增 + 广播队列串行 | ④ |
| 98 | harness 工具辅助 | agent/src/harness/utils/(shell-output/truncate) | ShellCaptureProgress + truncateTail | ② |
| 99 | SQLite repo 集成 | sqlite/repo.ts(953) | **writer lease 集成**:activeWriterError vs lostWriterError(租约失败语义区分) | ④ |
| 100 | 存储迁移 | sqlite/migrations/001_initial.sql | schema 全貌:entries 主键/UNIQUE seq/session_stats | ④ |
| 101 | 客户端状态 | client/src/(state/promise/session-handle) | ClientState 快照监听/会话附件/重置 | ④ |
| 102 | 遥测抽象 | telemetry/src/(index/memory/noop) | Span 抽象 + **敏感属性元数据**(sensitive/cardinality) | 可观测 |
| 103 | 评测 A/B 汇总 | evals/src/vitest-evals/(summary/harness-table/artifacts) | **HarnessObservation 5 态**(scored/unscored/skipped/pending/errored);baseline/candidates/repetition | ③评测 |
| 104 | 测试 provider | ai/src/providers/faux.ts(708) | 确定性 mock provider(测试基础设施) | — |

**v10/v11 元教训**:
- **工具延迟加载三项目**:Pi deferred-tools ↔ Hermes tool_search ↔ Reasonix 动态注册——"按需加载工具"是上下文节省通用模式
- **搜索三形态**:Pi 扫描式 ↔ Hermes FTS5 降级链 ↔ Reasonix BM25——产品④检索可组合三种
- **writer lease 错误语义**:activeWriter vs lostWriter(两种失败)——与 Hermes compression_lock holder 检查同思想
- **遥测敏感属性元数据**:Pi sensitive ↔ Hermes monitoring redaction ↔ Reasonix secrets——遥测必须声明敏感属性
- **"探索完毕"永远需要体量排序 + 逐目录核对双重验证**(#24/D18 三项目实证:Pi 59→108、Reasonix 41→101、Hermes 27→80)

### 🟢 v12-v14 新增(续扫第 12-14 轮 — rpc/资源仲裁/OAuth/遥测 schema,收官)

**触发**:继续逐文件深挖核心实现(rpc-mode 完整协议 → package-manager 资源仲裁 → output-guard 背压 → server 细节 → OAuth 实现 → 遥测 schema → provider 注册),第五轮确认无新域。

| # | 域 | 位置 | 设计要点 |
|---|----|------|---------|
| 105 | RPC 模式完整协议 | modes/rpc/(rpc-mode 817/rpc-client 601/rpc-types) | headless JSON stdin/stdout:takeOverStdout/命令+id/事件流/扩展 UI 往返/背压 |
| 106 | 资源优先级仲裁 | core/package-manager.ts:178-215 | resourcePrecedenceRank:package 4 > project-local 0 > project-remote 1 > user-local 2 > user-remote 3 |
| 107 | stdout 背压防护 | core/output-guard.ts | takeOverStdout + ENOBUFS/EAGAIN 重试 + flushRawStdout |
| 108 | 服务端活会话管理 | server/sessions.ts | LiveSessionManager:连接集/操作计数/ready/terminal/广播快照 |
| 109 | 协议 schema 契约 | protocol/src/schemas.ts(450) | TypeBox 严格对象 schema 族 = 协议单一真相源 + PROTOCOL_VERSION |
| 110 | 遥测适配器一致性 | telemetry/src/testing/conformance.ts | TelemetryAdapterConformanceCase(测试即契约) |
| 111 | 声明式遥测 schema | telemetry/src/index.ts:25-80 | TelemetrySchemaDefinition:version/spans 定义/父约束/**required 必填**/**sensitive**/cardinality/values 枚举——遥测契约化(最完整) |
| 112 | OAuth 设备码实现 | ai/src/auth/oauth/(device-code/pkce) | RFC 8628:slow_down +5s/默认 5s/min 1s/WSL 时钟提示;PKCE Web Crypto |
| 113 | 评测 harness 细节 | evals/src/pi-harness.ts | resolveModelSelection(PI_PROVIDER/MODEL);toTranscriptEvents;promptAgent 断言 stopReason==="stop" |

**v12-v14 元教训(收官)**:
- **资源优先级仲裁是通用需求**:Pi resourcePrecedenceRank ↔ Reasonix manifest ↔ Hermes 信任矩阵——"同名资源冲突的确定性仲裁"
- **headless 协议三实现**:Pi rpc-mode ↔ Hermes ACP/tui_gateway ↔ Reasonix acp/eventwire
- **stdout 背压**:Pi ENOBUFS 重试 ↔ Hermes stream_single_writer——协议输出不损坏
- **遥测契约化方向**:Pi 声明式 schema 最完整(span 定义+敏感+必填+枚举)
- **Pi 收官确认**:341 源码文件全部核对,剩余(tui 原生层/视觉/providers 实现/生成物)为排除清单——**Pi 117 域是真收敛**

### 🟢 v15-v19 新增(续扫第 15-19 轮 — agent 门面/双层循环细节/tui 原生层/server/测试契约)

**触发**:用户继续追问("感觉到底"再次证伪)——v15 打开 packages/agent 核心有真发现;v16 修正 tui 排除清单;v17-v19 扫 server 与测试契约族。

| # | 域 | 位置 | 设计要点 |
|---|----|------|---------|
| 114 | Agent 门面契约 | agent/src/(agent.ts 592/types.ts 443) | StreamFn/ToolExecutionMode/QueueMode/BeforeToolCall/AfterToolCall/ShouldStopAfterTurn/PrepareNextTurn 钩子;默认 convertToLlm(只留 user/assistant/toolResult) |
| 115 | 键盘输入解析 | tui/src/keys.ts(1,401) | Kitty 键盘协议 + 旧序列兼容(ctrl+[ = ESC 重叠处理) |
| 116 | 分片序列缓冲 | tui/src/stdin-buffer.ts(444) | **stdin 分片到达缓冲**:鼠标 SGR 序列分 3 片到达 → 缓冲到完整;转义超时/粘贴括号——**通用性高(任何流式协议输入)** |
| 117 | 撤销栈 | tui/src/undo-stack.ts | **clone-on-push 语义**(structuredClone 快照) |
| 118 | Emacs kill-ring | tui/src/kill-ring.ts | 环形缓冲 + 连续 kill 累积 + yank-pop |
| 119 | 终端协议协商 | tui/src/terminal.ts(559) | Kitty 查询/响应解析(标志 7)/协商前缀检测/进度序列 |
| 120 | 字节连接抽象 | server/src/connection.ts | ordered byte connection;ConnectionStage 状态机(awaitingHello→handshaking→ready→closing→closed) |
| 121 | 服务器协议 | server/src/protocol.ts(382) | 协议层(编解码 + 阶段推进) |
| 122 | Unix 传输 | server/src/transports/unix/(listener 434) | Unix socket 监听器 |

**v15-v19 深化契约(测试即行为契约)**:
- **重试语义**(agent-session-retry):瞬态重试成功/耗尽发失败/**prompt 等待重试完成**
- **压缩扩展协议**(compaction-extensions):before_compact 可取消/自定义压缩/**扩展抛错回退默认**/多扩展按序
- **动态工具注册**(agent-session-dynamic-tools):注册后刷新注册表/**promptSnippet 缺失 → 保留但不可见**
- **运行时生命周期事件**(runtime-events):session_before_switch 可取消/顺序契约(shutdown→beforeInvalidate→rebind)
- **双层循环细节**(agent-loop):**截断即全失败**(length → 所有工具失败 + "Re-issue with complete arguments")/prepareNextTurn 钩子/工具执行模式决策(任一 sequential → 整批串行)/steer 先于循环/follow-up 外层轮询
- **provider 兼容契约**(ai 137 测试):宽松 content(null→空数组)/工具调用 id 规范化/思考交错/缓存亲缘 e2e

**v15-v19 元教训(最终)**:
- **"感觉到底"不可信是 Pi 第六次实证**:v14 称"收官无新域",v15 换层面立刻有真发现——**穷尽性 = 逐文件核对 + 逐层面打开,无终点捷径**
- **排除清单也要复核**:tui 原生层曾排除,stdin-buffer 分片缓冲/undo-stack 快照实为通用设计(修正为部分保留)
- **扩展点必须有默认回退**:扩展抛错 → 回退默认(压缩扩展契约)
- **"注册 ≠ 可见"分离**:promptSnippet 缺失 → 保留但不暴露给模型

### 🟢 v20-v28 新增(续扫第 20-28 轮 — 测试契约全覆盖 + 核心实现层内部)

**触发**:用户继续追问。v20-v21 达成全部测试文件覆盖(474 个,除 tui 视觉);v22-v28 打开核心实现层内部(settings/resource-loader/model/composer/sdk/compaction 细节)。

| # | 域 | 位置 | 设计要点 |
|---|----|------|---------|
| 123 | 会话资源清理注册表 | ai/src/session-resources.ts | registerSessionResourceCleanup/cleanupSessionResources(会话结束统一清理,错误聚合 AggregateError) |

**v20-v28 深化契约(测试即行为契约,契约深化 30+)**:
- **conformance 工厂**:createSessionBackendConformance(跨后端一致性测试工厂——测试即契约终极形态)
- **Fencing 行为测试**:"fences a stale owner after an expired lease is acquired by another writer"(接管后旧 owner 被栅栏隔离)
- **重试语义**:瞬态重试成功/耗尽发失败/prompt 等待重试完成
- **压缩扩展协议**:before_compact 可取消/扩展抛错回退默认/多扩展按序
- **动态工具**:注册后刷新注册表/promptSnippet 缺失 → 保留但不可见
- **client disposal**:断开 → 子句柄失效 + 挂起请求拒绝
- **协议严格性**:拒绝有损转换/外来 tool id
- **server 会话**:快照修订串行化/广播只给附加客户端/单例运行时控制
- **SDK 装配**:会话恢复优先于配置/模型回退链/思维级别按模型钳制/恢复失败不静默
- **settings-manager**:修改追踪(只写改过的字段)/双作用域合并(全局+项目,项目不可信不加载)/版本迁移 4+
- **resource-loader**:上下文分层加载(全局→祖先→项目)+ 遮蔽检测(最内层优先)/全覆盖 override
- **model-runtime**:可用性快照代际检查/凭证操作串行化/凭证同步链路(错误包完整诊断)
- **compaction 切点**:findCutPoint(合法切点+token 反向扫描+边界回退)/摘要防跑偏提示词
- **provider-composer**:composeModelProvider 组装工厂/登录后可改模型(modifyModels)

**v20-v28 元教训(最终收官)**:
- **conformance 工厂是测试即契约的终极形态**:一个工厂生成 N 个后端的同一套契约(Pi 三项目中最通用)
- **"快照代际检查"三项目共证**:Pi availabilityRefreshSeq ↔ Hermes routing_generation ↔ Reasonix uihub 代际隔离
- **"会话恢复优先于配置"**:跨 session 延续的正确顺序(与 #21 同思想)
- **"上下文遮蔽优先于合并"**:settings 是合并(全局底+项目盖),context 是遮蔽(最内层优先)——两种分层规则互补
- **Pi 收官**:源码 341 文件 + 测试 474 文件(除视觉)+ 核心实现层(settings/resource-loader/model/composer/sdk/compaction)全部覆盖;剩余(agent-session 状态机内部/工具实现)已有 q1/q5 笔记覆盖——**127 域 + 30+ 契约深化,程序化三清单(文件×测试×实现层)全部通过**

### 🟢 v29-v32 深化(续扫第 29-32 轮 — harness 实现层/agent-harness 主体/工具对比/Q1 闭环)

**触发**:用户继续追问。v29 打开 v5 就标记"剩余未深挖"的 harness/session 实现层;v30 agent-harness 主体;v31 回答 v1 遗留 Q1;v32 验证 q1 源码。

**v29-v32 深化契约**:
- **原子发布**(jsonl/storage.ts):完整临时文件 + 原子 rename,崩溃只留 .tmp(三项目对比:Hermes atomic_write_text/Reasonix fileutil)
- **类型白名单解码**(jsonl/codec.ts):ENTRY_TYPES 7/RECORD_TYPES 9/OPERATION_KINDS 3,未知类型拒绝
- **状态推导管线**(context.ts):deriveSessionContextState(thinking/model/tools 从条目推导)→ 压缩替换(只留摘要+尾巴)→ 投影(deferred 过滤/custom 投影器)
- **AgentLane 契约全集**(agent-harness.ts):18+ 操作(prompt/skill/steer/followUp/nextRun 三队列/compact/navigateTree/resume/abort/watch)+ **14 个结构化 TaggedError**(LaneBusy/NoActiveRun/NothingToResume/…)
- **Q1 闭环**(两套工具系统):core = harness 超集(编码增强:高亮/图像/stdin 命令传输/预览节流),同一 AgentTool 契约
- **agent-session 验证**(q1 源码级):压缩中拒绝提交 prompt/扩展命令流式期间可执行/input 拦截先于展开

**Pi 最终收官(23 轮续扫)**:
- 源码实现面全覆盖:agent-session(32)/harness session(29)/harness 主体(30)/工具两套(31)/settings(25)/resource-loader(26)/model(27)/composer(28)/sdk(24)/compaction(23)/main(23)/server(17)/tui 原生(16)/agent 门面+循环(15)
- **127 域 + 40+ 契约深化**,源码/测试/实现层三清单全部通过
- 剩余仅排除清单(视觉/生成物/同构 providers)
- **"感觉到底"在 Pi 上 7 次被证伪后的最终收敛**:程序化覆盖核对(文件×测试×实现层)才是可信判据
