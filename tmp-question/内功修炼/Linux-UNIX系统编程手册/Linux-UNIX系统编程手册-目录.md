# 《Linux/UNIX 系统编程手册（第2版）》—— 完整目录

> 11章 + 2附录 | 系统编程圣经级著作 | 文件I/O→进程→线程→内存→信号→时间→epoll→mmap

---

### 第1章 入门和基本概念
1.1 系统编程（为什么要学/基础/C系统调用/C库调用）
1.2 API 和 ABI
1.3 标准（POSIX和SUS历史/C语言标准/Linux和标准）
1.4 本书的概念（文件/程序/进程/线程/用户和组/权限/信号/进程间通信/错误处理）

### 第2章 文件 I/O
2.1 打开文件（open()/creat()/新建文件所有者）
2.2 通过read()读取数据（返回值/非阻塞读/错误码/大小限制）
2.3 调用write()写入（Partial Write/Append模式/非阻塞写/错误码/大小限制）
2.4 同步 I/O 行为（fsync()/fdatasync()/sync()/O_SYNC/O_DSYNC/O_RSYNC）
2.5 直接 I/O
2.6 关闭文件 / 用lseek()查找 / 定位读写 / 文件描述符
2.7 I/O 多路复用（select()/poll()/区别）
2.8 内核内幕（虚拟文件系统VFS/页缓存/页回写）

### 第3章 缓冲 I/O
3.1 用户缓冲 I/O / 标准 I/O
3.2 通过文件描述符打开流 / 关闭流
3.3 从流中读取数据（每次读一字节/每次读一行/读二进制）
3.4 向流中写入数据（写入单个字符/写入字符串/写入二进制）
3.5 定位流 / Flush流 / 错误和文件结束
3.6 控制缓冲 / 线程安全（手动文件加锁/流解锁）/ 对标准I/O的批评

### 第4章 高级文件 I/O
4.1 分散/聚集 I/O（readv/writev）
4.2 Event Poll (epoll)——创建epoll实例/控制epoll/等待事件/边缘触发与条件触发
4.3 存储映射（mmap()/munmap()/实例/优点/不足/调整权限/调整大小/映射同步文件）
4.4 普通文件 I/O 提示（posix_fadvise()/readahead()）
4.5 同步（Synchronized）、同步（Synchronous）、异步（Asynchronous）操作
4.6 I/O 调度器和 I/O 性能（磁盘寻址/I/O调度器功能/选择调度器/优化性能）

### 第5章 进程管理
5.1 程序、进程和线程
5.2 进程 ID（分配/进程体系/pid_t/获取ID）
5.3 运行新进程（exec系统调用/fork()系统调用）
5.4 终止进程（终止方式/atexit()/on_exit()）
5.5 等待特定进程（wait()/wait3()/wait4()/其他方法）
5.6 用户和组（有效用户ID/保留用户ID/改变方式）
5.7 会话（Session）/ 守护进程

### 第6章 高级进程管理
6.1 进程调度（时间片/CFS调度进程/抢占式调度）
6.2 完全公平调度器（CFS）
6.3 让出处理器 / 进程优先级（nice()/getpriority()/setpriority()）
6.4 处理器亲和力（Affinity）
6.5 实时调度（硬实时软实时/延迟抖动截止期限/Linux实时支持/注意事项）
6.6 资源限制（limit/获取设置限制）

### 第7章 线程
7.1 多线程程序、进程和线程（多线程代价/其他选择）
7.2 线程模型（用户级/混合/协同程序）
7.3 线程模式（每连接一线程/事件驱动线程模式）
7.4 并发性、并行性和竞争 / 互斥（死锁）
7.5 Pthreads Linux 线程实现（API/链接/创建/线程ID/终止/join/detach）

### 第8章 文件和目录管理
8.1 文件及其元数据（stat/权限/所有权/扩展属性）
8.2 目录（获取当前工作目录/创建/获取内容/读取）
8.3 链接（硬链接/符号链接/解除链接/移动）
8.4 拷贝和移动文件
8.5 设备节点 / inotify（初始化/监视事件/高级选项/获取事件队列/删除监视）

### 第9章 内存管理
9.1 进程地址空间（地址空间调度/内存区域）
9.2 动态内存分配（malloc()/释放/调整大小/数据段管理）
9.3 匿名内存映射 / 映射到文件
9.4 高级内存分配（获取统计信息）
9.5 基于栈的分配 / 内存操作
9.6 内存锁定（锁定部分地址空间/全部地址空间/锁的限制）
9.7 内存分配策略

### 第10章 信号
10.1 信号相关的概念（语义/名称与编号）
10.2 基本信号管理（等待信号/信号捕获函数）
10.3 发送信号（kill()/进程组发送/终端进程发送）
10.4 信号集 / 信号掩码（信号集函数/等待信号集）
10.5 高级信号管理（sigaction()/发送附带信息信号/定时器/同步处理）

### 第11章 时间与定时器
11.1 时间的数据结构（基本类型/秒级纳秒精度/字符串转换/进程时间类型）
11.2 POSIX 时钟
11.3 获取当前时间（获取进程时间/线程时间）
11.4 设置时间 / 时钟高级接口
11.5 睡眠（秒级/纳秒级/高级方法）
11.6 间隔定时器（简单闹钟/高级定时器）

### 附录
- 附录 A：C 语言的 GCC API
- 附录 B：参考书目
